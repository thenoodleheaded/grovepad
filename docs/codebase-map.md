# Grovepad codebase map

This is the compact navigation layer for fresh AI tasks. It routes user language to the smallest useful source surface. For system diagrams, fan-in/fan-out, cycles, and deep invariants, continue to the [architecture map](architecture-map.md).

## How to use this map

1. Search task words here before opening source: `rg -n -i "panel|dependency|quick add" docs/codebase-map.md`.
2. Start with the first file in the matching route.
3. Search the listed symbols/classes inside those files and read narrow ranges.
4. Inspect direct consumers only when the requested behavior crosses the stated owner.
5. Run the route's targeted check before escalating to the full suite or browser.

Paths and headings are checked by `npm run docs:check`. Use symbol names rather than line numbers because lines drift.

## Task router

| User language / task | Start here | State or contract owner | Styles / UX law | Nearest verification |
|---|---|---|---|---|
| App boot, login, guest route | [App.tsx](../src/App.tsx), [LoginPage.tsx](../src/components/auth/LoginPage.tsx) | [useAuthStore.ts](../src/store/useAuthStore.ts) | [index.css](../src/index.css) | Build plus guest/signed-in smoke |
| Canvas composition, global shortcuts, overlay mounting | [CanvasViewport.tsx](../src/components/canvas/CanvasViewport.tsx) | Runtime start/stop: [appRuntime.ts](../src/runtime/appRuntime.ts); canvas/store owners below | [product.css](../src/styles/product.css) | Build, lint, applicable smoke |
| Pan, zoom, camera history, pointer selection | Camera core: [cameraEngine.ts](../src/engine/camera/cameraEngine.ts); gestures: [gestureEngine.ts](../src/engine/camera/gestureEngine.ts); public seam: [useCanvasStore.ts](../src/store/useCanvasStore.ts) | Gesture decision table: [canvasGesturePolicy.ts](../src/utils/canvasGesturePolicy.ts); fling math: [glidePhysics.ts](../src/engine/camera/glidePhysics.ts); content-anchored world rect for SVG layers (deliberately camera-independent, so panning never re-lays-out an edge layer): [useWorldContentRect.ts](../src/hooks/useWorldContentRect.ts); [canvasView.ts](../src/utils/canvasView.ts) | Contract: [canvas engine](canvas-engine.md) | Engine/physics/policy tests, typecheck, then pan/zoom/select smoke |
| Adaptive input modality, interaction modes, viewport class, safe-area chrome | [adaptiveInputRuntime.ts](../src/runtime/adaptiveInputRuntime.ts), [useAdaptiveInputStore.ts](../src/store/useAdaptiveInputStore.ts) | Pure capability rules: [adaptiveInput.ts](../src/utils/adaptiveInput.ts); compact chrome decisions: [adaptiveChrome.ts](../src/utils/adaptiveChrome.ts); safe-area/target tokens: [01-tokens-base.css](../src/styles/product/01-tokens-base.css) | Capability detection uses pointer/media/visual viewport signals, never user-agent device guesses | Closest adaptive-input/chrome test, typecheck, then responsive browser smoke |
| Responsive canvas toolbar, workspace menu, canvas tree, accessible spatial outline, search, capture, imports, minimap, zoom, controls reference | [CanvasToolbar.tsx](../src/components/ui/CanvasToolbar.tsx), then the named surface in [components/ui](../src/components/ui); the controls reference is embedded in [SettingsPanel.tsx](../src/components/ui/SettingsPanel.tsx) | Legal canvas moves: [canvasTreePolicy.ts](../src/utils/canvasTreePolicy.ts); semantic tree order/navigation: [canvasOutline.ts](../src/utils/canvasOutline.ts); adaptive chrome: [adaptiveChrome.ts](../src/utils/adaptiveChrome.ts); overlay state: [uiLinkingSlice.ts](../src/store/slices/uiLinkingSlice.ts) | Follow the [popup panel design standard](popup-panel-design.md) for temporary panels; search `gp-command`, `gp-widget-picker`, `gp-quick-add`, `gp-import`, `gp-settings`, and safe-area classes in [product.css](../src/styles/product.css) | Policy/outline/contract tests, typecheck, lint, then keyboard-traverse and open/close every overlay in browser |
| Widget shell drag, resize, icon scaling, selection, ports, magnetic hover | [WidgetCard.tsx](../src/components/widgets/WidgetCard.tsx), [useWidgetResize.ts](../src/components/widgets/useWidgetResize.ts), [useWidgetMagneticHover.ts](../src/components/widgets/useWidgetMagneticHover.ts) | Input arbitration (`isInteractiveWidgetTarget` must stay `Element`-wide, or icon-only controls read as drag surface): [widgetPointerPolicy.ts](../src/utils/widgetPointerPolicy.ts); outline-proximity hit geometry: [widgetResizeEdge.ts](../src/utils/widgetResizeEdge.ts); icon scaling (a square dragged continuously across `ICON_MIN_EDGE`–`ICON_MAX_EDGE`, then snapped on release to a 2×2 or 3×3 grid square without intermediate size states; helpers `clampIconEdge`/`snapIconEdgeToGrid`/`crushesToIcon`/`iconEscapesToFull` in [widgetScale.ts](../src/utils/widgetScale.ts)) and `sizing.fixed` in [spatial.ts](../src/types/spatial.ts) / [contracts/registry.ts](../src/widgets/contracts/registry.ts); [useWidgetStore.ts](../src/store/useWidgetStore.ts); bounded pointer geometry: [widgetMagnetism.ts](../src/utils/widgetMagnetism.ts); glue bond/seam geometry: [glueGeometry.ts](../src/utils/glueGeometry.ts) | Search `gp-widget-card`, `gp-resize-edge`, `data-resize-edge`, `data-elastic`, `data-unglue-intent`, and `data-magnetic-hover` in [index.css](../src/index.css) and [product.css](../src/styles/product.css) | Closest pointer/glue tests, build, plus mouse/touch/Pencil drag-resize-cancel smoke |
| Apple Pencil, pressure ink, Sketchpad, palm rejection, hover preview, Scribble-safe text entry | [SketchpadWidget.tsx](../src/components/widgets/modules/SketchpadWidget.tsx) | Persisted points/strokes: [widgetDataCore.ts](../src/types/widgetDataCore.ts); pure sampling/width/simplification/eraser policy: [sketchpadStroke.ts](../src/utils/sketchpadStroke.ts); text-entry arbitration: [widgetPointerPolicy.ts](../src/utils/widgetPointerPolicy.ts) | Article XIII in [widget constitution](widget-constitution.md); search `gp-sketch`, `data-pencil-drawing`, and Scribble comments in [product.css](../src/styles/product.css) | Stroke/pointer/history/contract tests, typecheck, then draw/erase/undo/redo/reload hardware smoke |
| Settings, keyboard and gesture reference, device-local UI preferences | [SettingsPanel.tsx](../src/components/ui/SettingsPanel.tsx), [ShortcutsOverlay.tsx](../src/components/ui/ShortcutsOverlay.tsx) | [useSettingsStore.ts](../src/store/useSettingsStore.ts); theme state: [useThemeStore.ts](../src/store/useThemeStore.ts); browser zoom and viewport breakpoints own interface scale | [Popup panel design standard](popup-panel-design.md); `.gp-settings-*` and preference-driven `data-*` rules in product styles | Settings contract test, typecheck, lint, then desktop/tablet/phone settings smoke |
| Whole-card content-safe sizing, grow-only adjustment, icon state | [widgetContentFloor.ts](../src/utils/widgetContentFloor.ts), [WidgetCard.tsx](../src/components/widgets/WidgetCard.tsx) | Registry fallback windows: widget registry; ephemeral mounted floors: [liveWidgetSizing.ts](../src/store/liveWidgetSizing.ts); clamp/state: [widgetLayoutSlice.ts](../src/store/slices/widgetLayoutSlice.ts) | Article XII.1 in [widget constitution](widget-constitution.md), Article XVIII in [glass constitution](widget-glass-constitution.md) | content-floor, scale-state, and sizing-profile tests; `npm run check:full`; narrow/wide/icon smoke |
| Widget contents, one specific widget, or a widget mode family | Search the type in the matching family map under [renderers](../src/components/widgets/renderers), then open the named file in [modules](../src/components/widgets/modules); consolidated mode adapters live in [consolidatedWidgetRenderers.tsx](../src/components/widgets/renderers/consolidatedWidgetRenderers.tsx) | Family maps own typed dispatch; [consolidatedWidgetData.ts](../src/utils/consolidatedWidgetData.ts) owns generated legacy-data conversion; [WidgetRenderer.tsx](../src/components/widgets/WidgetRenderer.tsx) owns suspense, the forward-compatibility placeholder, and the responsive shell | Both constitutions; shared island vocabulary in [WidgetPanel.tsx](../src/components/widgets/WidgetPanel.tsx) | Renderer-family ownership and widget-consolidation tests, closest widget test, then build |
| Widget skins (the alternate shapes one card wears), the title-anchored skin roller, per-skin accents, haptic detents | [WidgetSkinRoller.tsx](../src/components/widgets/WidgetSkinRoller.tsx), opened by the title-capsule icon in [WidgetCard.tsx](../src/components/widgets/WidgetCard.tsx) via [useWidgetSkinSwitch.ts](../src/components/widgets/useWidgetSkinSwitch.ts) | One owner answers what skins a card has, which it wears, what data wearing another produces, and what colour it takes: [widgetSkins.ts](../src/utils/widgetSkins.ts). Registry widgets declare `skins` (each with its own `accent`); the Tracker's skins are the whole [Atlas catalogue](../src/widgets/atlasCatalog.ts) and switching snapshots the outgoing preset. The worn skin is persisted board data (`data.mode`, `data.trackerMode`) and keeps those names — the storage contract does not rename. Drum physics, detents, wheel stepping, end resistance, barrel placement: [skinRollerGeometry.ts](../src/components/widgets/skinRollerGeometry.ts); every physical tick goes through [haptics.ts](../src/utils/haptics.ts) — the one seam that will drive phone vibration | Search `gp-skin-roller` in [02-canvas-toolbar.css](../src/styles/product/02-canvas-toolbar.css); `gp-skin-roller-behind` is the blur applied to the app root while the drum is open | [skinRollerGeometry.test.ts](../src/components/widgets/skinRollerGeometry.test.ts), then roll by drag, wheel, and arrow keys on both a short list and the Tracker, and confirm Esc leaves the skin unchanged |
| Add-widget picker, catalogue grouping, widget defaults, favorites, per-pack widget curation | [AddWidgetModal.tsx](../src/components/ui/AddWidgetModal.tsx) | [registry.ts](../src/widgets/registry.ts) and registry families for catalogue data; device-local favorite/hidden-pack-widget prefs (not board state) in [useWidgetPickerPrefsStore.ts](../src/store/useWidgetPickerPrefsStore.ts) | Search `gp-widget-picker` in [product.css](../src/styles/product.css) | Build, lint, picker keyboard/pointer smoke |
| Light/dark mode, glass, global widget color system | [useThemeStore.ts](../src/store/useThemeStore.ts), [index.css](../src/index.css), [product.css](../src/styles/product.css) | CSS theme tokens and registry accents | [glass constitution](widget-glass-constitution.md) | `npm run check:full` plus both-theme visual smoke |
| Local calendar dates, relative-day defaults, due-date seeds | [localDate.ts](../src/utils/localDate.ts), then search `localDayKey` consumers | The local calendar helper owns `YYYY-MM-DD` day keys; registries and field/default producers own domain offsets | Date inputs stay local; never derive a day key with UTC `toISOString()` | [localDate.test.ts](../src/utils/localDate.test.ts), typecheck, positive-offset browser smoke |
| Board mutations, undo/redo, glue, hierarchy, untangle, auto-scale | [useWidgetStore.ts](../src/store/useWidgetStore.ts), then the named domain in [slices](../src/store/slices) | [widgetStoreTypes.ts](../src/store/widgetStoreTypes.ts); action bodies live in domain slices | [architecture map](architecture-map.md) invariants | Closest store test, then `npm run check` |
| Glue welds (0.3-cell seams), option-drag glue/unglue gesture, cluster drag-all, weld preview | [GlueSeamLayer.tsx](../src/components/widgets/GlueSeamLayer.tsx), [WidgetCard.tsx](../src/components/widgets/WidgetCard.tsx) | There is NO backplate and no group object: glued widgets stay individual cards welded edge-to-edge at a `GLUE_GAP` (0.3-cell, 12px) seam. Bond/seam/snap geometry is one owner: [glueGeometry.ts](../src/utils/glueGeometry.ts) (`findGlueSnap` picks the nearest facing widget within `GLUE_RANGE` = one cell; `glueSeamsForCluster` traces the merge shape — a bar for facing edges, a corner patch for diagonal elbows, so a row reads as —, an L as Γ, a 2×2 as a filled square). Cluster membership (`glues`, `widgetGlueIndex`) and the actions (`glueWidgets`, `unglueWidget`, `commitGlue`) live in [glueSlice.ts](../src/store/slices/glueSlice.ts); ephemeral option-drag intent (`glueIntent`, `unglueIntentWidgetId`) previews the exact bond a release would commit. Membership carries no adjacency, so `reconcileGlueClusters` (glueGeometry.ts, using `connectedGlueComponents`, edge = within `GLUE_RANGE`) re-derives clusters from what actually touches after every commit/unglue/delete — a member re-welded elsewhere or a deleted connector never leaves a record whose pieces float apart. A plain drag of any member moves the whole cluster (`moveWidget` expands through clusters; `soloGlued` is the option-drag exception) and `selectWidget` selects the whole cluster to match; settling and displacement treat a cluster as one rigid unit whose internal seams survive every snap ([widgetSettling.ts](../src/store/widgetSettling.ts) rigid cluster snap). `findGlueSnap` forgives a half-cell overshoot (`GLUE_OVERSHOOT`) so dragging a card onto a neighbour still welds; the weld target lights up via `[data-glue-target]`. | Glue law in [glass constitution](widget-glass-constitution.md); `.gp-glue-seam`, `[data-glue-preview]`, `[data-glue-fading]`, `[data-unglue-intent]` in [04-controls.css](../src/styles/product/04-controls.css) | [glueGeometry.test.ts](../src/utils/glueGeometry.test.ts), [glueSlice.test.ts](../src/store/glueSlice.test.ts), [glueSeamContracts.test.ts](../src/components/widgets/glueSeamContracts.test.ts), glued-card browser smoke |
| Destructive widget/canvas deletion, cascade disclosure, subtree Undo | [WidgetDeletionDialog.tsx](../src/components/ui/WidgetDeletionDialog.tsx), [useWidgetDeletionDialogStore.ts](../src/store/useWidgetDeletionDialogStore.ts) | Canonical blast-radius analysis: [widgetDeletion.ts](../src/store/widgetDeletion.ts); deletion transaction: [selectionSlice.ts](../src/store/slices/selectionSlice.ts) | Confirmation and toast copy locally | Deletion-impact store test plus dialog browser smoke |
| Relations, free-form relation policy, and semantic relation editing | [RelationLines.tsx](../src/components/canvas/RelationLines.tsx), touch-safe commands in [SelectionActionBar.tsx](../src/components/ui/SelectionActionBar.tsx) | Relation records and target-picking state in [useWidgetStore.ts](../src/store/useWidgetStore.ts); drawn direction and full-border attachment policy: [relationPolicy.ts](../src/utils/relationPolicy.ts). The old canvas-wide `relationStrict` field is compatibility data only and has no runtime effect. | Geometry: [curve.ts](../src/utils/curve.ts); target arbitration: [widgetPointerPolicy.ts](../src/utils/widgetPointerPolicy.ts); shared paint: [CanvasEdge.tsx](../src/components/canvas/CanvasEdge.tsx) | [relationPolicy.test.ts](../src/utils/relationPolicy.test.ts), pointer-policy, shared-edge/curve tests, free-form relation smoke |
| Dependencies, blocker direction, dependency chips | [DependencyLines.tsx](../src/components/canvas/DependencyLines.tsx) | Blocker relations in [useWidgetStore.ts](../src/store/useWidgetStore.ts) | Geometry: [dependencyGeometry.ts](../src/utils/dependencyGeometry.ts); shared paint: [CanvasEdge.tsx](../src/components/canvas/CanvasEdge.tsx) | Shared-edge and dependency geometry tests plus dependency smoke |
| Input/output ports, wires, connection propagation | [PortRail.tsx](../src/components/widgets/PortRail.tsx), [WireLayer.tsx](../src/components/canvas/WireLayer.tsx), Connect mode in [CanvasModeDock.tsx](../src/components/ui/CanvasModeDock.tsx) | [fields.ts](../src/widgets/fields.ts), [circuitEngine.ts](../src/engine/circuitEngine.ts), [useCircuitStore.ts](../src/store/useCircuitStore.ts) | Tap/drag slop: [pointerTap.ts](../src/utils/pointerTap.ts); geometry: [portGeometry.ts](../src/utils/portGeometry.ts); paint: [CanvasEdge.tsx](../src/components/canvas/CanvasEdge.tsx); [circuit-engine.md](circuit-engine.md) | Pointer-tap, circuit-store, shared-edge, connection lifecycle tests plus tap-to-connect/drag-wire smoke |
| Persistence, visible save/offline state, installable offline shell, device state, reload, migration snapshots, IndexedDB, cloud sync, stale deploys | [persistence.ts](../src/utils/persistence.ts), [AccountChip.tsx](../src/components/ui/AccountChip.tsx) | Status wording: [persistenceStatus.ts](../src/utils/persistenceStatus.ts); network/service-worker lifecycle: [networkStatusRuntime.ts](../src/runtime/networkStatusRuntime.ts); web manifest and cache policy: [manifest.webmanifest](../public/manifest.webmanifest), [service-worker.js](../public/service-worker.js); board schema: [persistedBoardSchema.ts](../src/utils/persistedBoardSchema.ts); local navigation: [persistedDeviceState.ts](../src/utils/persistedDeviceState.ts); protected snapshot replacement/rollback: [snapshotRestore.ts](../src/utils/snapshotRestore.ts); cloud reconciliation: [cloudSync.ts](../src/utils/cloudSync.ts); conflict workspace merge: [boardWorkspaceMerge.ts](../src/utils/boardWorkspaceMerge.ts); portable `.grovepad` package: [grovepadPackage.ts](../src/utils/grovepadPackage.ts); IndexedDB: [boardDatabase.ts](../src/utils/boardDatabase.ts); deploy monitor: [deployVersionMonitor.ts](../src/runtime/deployVersionMonitor.ts) | [Storage format plan](storage-format-plan.md) and persistence flow in [architecture map](architecture-map.md) | status/runtime tests, board/device/cloud tests, production build, manifest/service-worker fetch, online/offline/reload smoke |
| Link previews, page title/description, crawler rules, canonical URL | [index.html](../index.html) | Preview card art: [og-image.png](../public/og-image.png) (1200×630, regenerated by hand, not built); crawler rules: [robots.txt](../public/robots.txt), [sitemap.xml](../public/sitemap.xml); cache/security headers: [_headers](../public/_headers) | Single-page app: every path serves the same shell, so the canonical tag stays `https://grovepad.app/` and collaboration invite links stay disallowed | `npm run build`, then confirm `dist/` contains `robots.txt`, `sitemap.xml`, `og-image.png` and the `og:` tags survive in `dist/index.html` |
| Multiplayer, live cursors, shared selection, follow mode, collaborative notes, invitations, roles, comments, reconnect | [collaborationRuntime.ts](../src/runtime/collaborationRuntime.ts), [CollaborationOverlays.tsx](../src/components/collaboration/CollaborationOverlays.tsx) | Share registration/recovery policy: [canvasSharing.ts](../src/collaboration/canvasSharing.ts); CRDT schema/validation: [yjsCanvas.ts](../src/collaboration/yjsCanvas.ts); canonical-store bridge: [canvasStoreBridge.ts](../src/collaboration/canvasStoreBridge.ts); UI state: [useCollaborationStore.ts](../src/store/useCollaborationStore.ts); follow-mode reconciliation (a departed target must release the camera, which follow holds via the gesture engine): [followTarget.ts](../src/collaboration/followTarget.ts); durable/offline transport: [supabaseCollaboration.ts](../src/collaboration/supabaseCollaboration.ts), [offlineUpdateQueue.ts](../src/collaboration/offlineUpdateQueue.ts); authorization: [realtime collaboration migration](../supabase/migrations/20260719050000_realtime_collaboration.sql) and [awareness-channel migration](../supabase/migrations/20260722093000_realtime_awareness_channel.sql) | [Realtime collaboration runbook](realtime-collaboration.md); collaboration styles in [07-collaboration.css](../src/styles/product/07-collaboration.css) | Collaboration + pressure suites, `npm run check:full`, database policy tests, then signed-in two-browser/reconnect/role smoke |
| AI Generator widget, generated canvas output | [AiGeneratorWidget.tsx](../src/components/widgets/modules/AiGeneratorWidget.tsx) | [widgetGeneration.ts](../src/services/widgetGeneration.ts), then [localAiService.ts](../src/services/localAiService.ts) | Widget status UI locally; plan validation stays in the AI service | [widgetGeneration.test.ts](../src/services/widgetGeneration.test.ts), local-AI tests, generation/cancel smoke |
| Panel/list removal animation or copied acknowledgement | [WidgetPanel.tsx](../src/components/widgets/WidgetPanel.tsx), [useTransientValue.ts](../src/hooks/useTransientValue.ts) | Stable item ids in widget data; timeout ownership in [ownedTimeout.ts](../src/utils/ownedTimeout.ts) | `gp-subpanel-exit` in [product.css](../src/styles/product.css) | Panel-removal and owned-timeout tests plus reduced-motion smoke |
| Quick Add, thought interpretation, scenario prediction, blueprint preview | [QuickAddSheet.tsx](../src/components/ui/QuickAddSheet.tsx), [QuickAddPreviewLayer.tsx](../src/components/canvas/QuickAddPreviewLayer.tsx) | [thoughtInterpreter.ts](../src/utils/thoughtInterpreter.ts), [scenarioResolver.ts](../src/utils/scenarioResolver.ts); shared parent-graph layout: [planLayout.ts](../src/utils/planLayout.ts); pure preview scene: [quickAddPreviewScene.ts](../src/components/canvas/quickAddPreviewScene.ts) | [scenario-intelligence.md](scenario-intelligence.md) | Scenario/scaffold/structural planner and preview-contract tests |
| Optional local AI, model profiles, deterministic/model handshake | [localAiService.ts](../src/services/localAiService.ts), [local-ai](../src/services/local-ai) | [planProtocol.ts](../src/services/local-ai/planProtocol.ts), runtime adapters | [local-ai-runtime.md](local-ai-runtime.md) | Local-AI, plan protocol, profile, and adapter tests |
| AI plugin, MCP, external mind-map/tree generation, bring-your-own AI account | [grovepad-mcp.mjs](../scripts/grovepad-mcp.mjs), [mcpBridgeRuntime.ts](../src/runtime/mcpBridgeRuntime.ts) | Preview lifecycle and consume-once commit: [useMcpConnectorStore.ts](../src/store/useMcpConnectorStore.ts); on-canvas review pill: [McpPreviewLayer.tsx](../src/components/canvas/McpPreviewLayer.tsx); browser validation and tree plans: [treeContract.ts](../src/mcp/treeContract.ts); loopback broker: [grovepadBridge.mjs](../scripts/mcp/grovepadBridge.mjs); opt-in: [useSettingsStore.ts](../src/store/useSettingsStore.ts) | [Grovepad MCP connector](mcp-connector.md); only the open app may call canonical board actions | MCP contract, preview-lifecycle, and bridge tests, typecheck, lint, then connect/preview/commit/Undo smoke |
| Widget layer mounting, ambient aura | [WidgetLayer.tsx](../src/components/widgets/WidgetLayer.tsx), [CanvasAuraLayer.tsx](../src/components/canvas/CanvasAuraLayer.tsx) | Every widget on the active canvas mounts a full [WidgetCard.tsx](../src/components/widgets/WidgetCard.tsx); the parent world transform does all per-frame camera work. The aura layer redraws from the camera store mirror. Each on-screen widget emits its own accent as a radial blob whose anchor lags the widget, so dragging never smears colour across the board | Global reduced-motion fail-safe in [index.css](../src/index.css) | Typecheck, build, circuit mode and reduced-motion smoke |
| Aura tuning knobs, accent overrides, canvas colour tuning | [auraTuning.ts](../src/components/canvas/auraTuning.ts) owns every number and the sanitizer; [useAuraTuningStore.ts](../src/store/useAuraTuningStore.ts) persists a draft; [AuraTuningPanel.tsx](../src/components/canvas/AuraTuningPanel.tsx) is the dev-only editor (reached from a button inside the Fine-tune menu, `import.meta.env.DEV`) | `DEFAULT_AURA_TUNING` is the single production source — the panel exports a document to paste back and bake in. Every pasted or restored value passes `sanitizeAuraDocument`, because a non-finite radius throws out of `createRadialGradient` | Canvas colours apply as inline custom properties via `applyCanvasColors` and are lifted on teardown, so the stylesheet stays authoritative | [auraTuning.test.ts](../src/components/canvas/auraTuning.test.ts), then aura smoke in both themes |
| Owner-facing appearance Fine-tune menu (blur/shadow/motion knobs), `G` shortcut | Catalogue + apply/sanitize: [uiTuning.ts](../src/components/ui/uiTuning.ts); persisted local overrides: [useUiTuningStore.ts](../src/store/useUiTuningStore.ts); panel: [UiTuningPanel.tsx](../src/components/ui/UiTuningPanel.tsx); `G` binding + mount in [CanvasViewport.tsx](../src/components/canvas/CanvasViewport.tsx) | Each `TuneField` binds one slider to one CSS custom property; a knob at its default writes nothing, so the stylesheet fallback (which MUST equal the field `default`) stays authoritative. Local only — never board data, history, or sync. Add a knob = one field here + one `var(--gp-tune-…, fallback)` in [index.css](../src/index.css). Ships in the real app, not dev-gated; the panel is `position: fixed` inline because `.gp-popup-surface` forces `position: relative` over layered Tailwind utilities | [uiTuning.test.ts](../src/components/ui/uiTuning.test.ts), typecheck, then open with `G`, drag a slider, Reset, reload smoke |
| Resting faces, click-to-expand tiles, energy-light widget rest state | Pure decision/geometry: [widgetRest.ts](../src/utils/widgetRest.ts); ephemeral view state: [useWidgetRestStore.ts](../src/store/useWidgetRestStore.ts); effective-geometry hook: [useEffectiveWidget.ts](../src/hooks/useEffectiveWidget.ts) | Every registry type rests by default; `restingFace: false` is the explicit safety escape hatch. Expansion is per-viewer view state — never history, persistence, or sync — and is centre-anchored (`restExpansionOffset`), so geometry consumers (edge layers, [PortRail.tsx](../src/components/widgets/PortRail.tsx)) must anchor to `widgetWithEffectiveSize`, not `effectiveWidgetSize` alone. An expansion remembers the state it opened from (`expandedFrom`: rest tile, or an icon's exact square) and folds back onto it on collapse — a card opened out of an icon returns to that very icon, history-neutrally (`toSize` on `setWidgetScaleState`); pinning releases the slot with `restoreOrigin: false` because pin means held open. Pinning (`metadata.pinned`, the card's Pin button) is the opposite of resting: a durable board property that holds a card open at its stored footprint, so a pinned widget is neither resting nor the expanded member | Resting face render path and expand/collapse cross-fade in [WidgetCard.tsx](../src/components/widgets/WidgetCard.tsx) (`.gp-rest-face`/`.gp-rest-content-in`, timed by `REST_TRANSITION_MS`); the expanded card floats one past the board's live top z with `.gp-rest-halo`, a three-cell backdrop-blur floor shadow over its neighbours (index.css); collapse wiring (Esc, canvas click) in [CanvasViewport.tsx](../src/components/canvas/CanvasViewport.tsx) | [widgetRest.test.ts](../src/utils/widgetRest.test.ts), [widgetPinned.test.ts](../src/store/widgetPinned.test.ts), typecheck, then click-expand/Esc/drag and pin/reload smoke |
| Per-type resting face art, content-derived tile sizing | [WidgetRestingFace.tsx](../src/components/widgets/WidgetRestingFace.tsx) | Face model + tile measurement (one contract): [restingFace.ts](../src/utils/restingFace.ts); chart geometry: [sparkline.ts](../src/utils/sparkline.ts). Faces are bounded (row/mark/char caps), so data size never changes render cost | The face shows real content sized to itself — rows/text/values, a bare icon cell when empty, the bare image for media (ratio-locked resting resize) — never counts, never a fixed tile | [restingFace.test.ts](../src/utils/restingFace.test.ts), [sparkline.test.ts](../src/utils/sparkline.test.ts), then per-mode resting/expand smoke |
| Timer/Pomodoro/Stopwatch dial, clock ring around a card, its icon glyph | [WidgetClockRing.tsx](../src/components/widgets/WidgetClockRing.tsx), [TimekeeperWidget.tsx](../src/components/widgets/modules/TimekeeperWidget.tsx) | One pure reading for every surface: [widgetClock.ts](../src/utils/widgetClock.ts) (+ [useWidgetClock.ts](../src/hooks/useWidgetClock.ts), which subscribes to the shared clock only while ticking); mark geometry: [squircleTicks.ts](../src/utils/squircleTicks.ts); every overlay a card paints on its own outline goes through [WidgetBoundsOverlay.tsx](../src/components/widgets/WidgetBoundsOverlay.tsx) | The card's own outline is the dial — the readout sits bare inside it, never in a nested well. A card's box *glides* between sizes while the renderer's numbers jump to the destination, so a bounds overlay states its geometry in a `viewBox` and fills the box; a pixel-sized `<svg>` hangs outside the card for the length of every transition. Search `gp-clock-ring` and `gp-clock-body` in [product.css](../src/styles/product.css) | [squircleTicks.test.ts](../src/utils/squircleTicks.test.ts), [widgetBoundsOverlayContracts.test.ts](../src/components/widgets/widgetBoundsOverlayContracts.test.ts), typecheck, then start/pause/reset and resize smoke |
| Imports, PDF/document parsing, generated mind-map topology, media blobs | [ImportDocumentModal.tsx](../src/components/ui/ImportDocumentModal.tsx) | [documentReader.ts](../src/utils/documentReader.ts), [pendingImport.ts](../src/utils/pendingImport.ts), [boardDatabase.ts](../src/utils/boardDatabase.ts); import adapter: [mindmapLayout.ts](../src/utils/mindmapLayout.ts), which reuses the parent-graph geometry in [planLayout.ts](../src/utils/planLayout.ts) and creates no group objects | Import UI locally | Mind-map layout test, build, plus import/cancel/reload smoke |
| Native desktop shell, `.grovepad` file association/icon, OS open-with, all-platform packaging | [src-tauri/src/lib.rs](../src-tauri/src/lib.rs) | Bundle/icon/fileAssociations config: [tauri.conf.json](../src-tauri/tauri.conf.json); macOS bundle extras: [tauri.macos.conf.json](../src-tauri/tauri.macos.conf.json); frontend bridge: [nativeFileOpen.ts](../src/runtime/nativeFileOpen.ts); package parse: [grovepadPackage.ts](../src/utils/grovepadPackage.ts); Canvas-card import/media orchestration: [boardCanvasImport.ts](../src/utils/boardCanvasImport.ts); graph embedding: [boardCanvasEmbedding.ts](../src/utils/boardCanvasEmbedding.ts); per-device build/signing runbook: [native-apps.md](native-apps.md) | N/A — native packaging, not a UI surface | `cargo check` in `src-tauri`, `npm run tauri:dev`, the ladder in [native-apps.md](native-apps.md), open-with smoke |
| Native Note home-screen widget, WidgetKit, Android Glance, device-local selected Note | [nativeNoteWidgetSync.ts](../src/runtime/nativeNoteWidgetSync.ts), [native-widget plugin](../src-tauri/plugins/native-widget), [GrovepadNoteWidget.swift](../src-tauri/native/apple/GrovepadNoteWidget.swift) | Device-only selection/status: [useNativeWidgetStore.ts](../src/store/useNativeWidgetStore.ts); bounded payload contract: [nativeNoteWidget.ts](../src/utils/nativeNoteWidget.ts); validated Tauri command: [src-tauri/src/lib.rs](../src-tauri/src/lib.rs); setup/runbook: [native-os-widgets.md](native-os-widgets.md) | Context-menu action in [WidgetContextMenu.tsx](../src/components/ui/WidgetContextMenu.tsx); native renderers own platform paint | Focused runtime/contract tests, `cargo test`, Android plugin `assembleDebug`, iOS + macOS extension builds; signed-device add/edit/remove smoke |

## Domain routes introduced in Phase 3

| Domain | Implementation route | Contract / pure helpers |
|---|---|---|
| Workspace and canvas hierarchy | [navigationSlice.ts](../src/store/slices/navigationSlice.ts) | [workspaces.ts](../src/types/workspaces.ts) |
| Widget creation and thought plans | [widgetCreationSlice.ts](../src/store/slices/widgetCreationSlice.ts) | [widgetSizing.ts](../src/store/widgetSizing.ts), [widgetSeeds.ts](../src/store/widgetSeeds.ts) |
| Move, resize, auto-scale, untangle, drag displacement preview | [widgetLayoutSlice.ts](../src/store/slices/widgetLayoutSlice.ts) | [widgetCollection.ts](../src/store/widgetCollection.ts), [widgetSettling.ts](../src/store/widgetSettling.ts), [widgetUntangle.ts](../src/store/widgetUntangle.ts), [spatialNegotiation.ts](../src/store/spatialNegotiation.ts), [dragDisplacement.ts](../src/store/dragDisplacement.ts) |
| Relations and circuit connections | [circuitSlice.ts](../src/store/slices/circuitSlice.ts) | [relations.ts](../src/types/relations.ts), [fields.ts](../src/widgets/contracts/fields.ts) |
| Glue | [glueSlice.ts](../src/store/slices/glueSlice.ts) | [widgetGraph.ts](../src/store/widgetGraph.ts) |
| Selection, deletion, duplication | [selectionSlice.ts](../src/store/slices/selectionSlice.ts) | [widgetStoreTypes.ts](../src/store/widgetStoreTypes.ts), [widgetDeletion.ts](../src/store/widgetDeletion.ts) |
| Overlays, linking, ghost tree shaper, per-node widget bundles | [uiLinkingSlice.ts](../src/store/slices/uiLinkingSlice.ts), [GhostTreeShaper.tsx](../src/components/canvas/GhostTreeShaper.tsx), selection mode in [AddWidgetModal.tsx](../src/components/ui/AddWidgetModal.tsx) | Multi-root variable bundle layout: [widgetGhostLayout.ts](../src/store/widgetGhostLayout.ts); icon packing, adaptive contours, and alternating accent dashes: [ghostTreePresentation.ts](../src/utils/ghostTreePresentation.ts); staged branch/widget reveal: [treeReveal.ts](../src/store/treeReveal.ts) |
| Undo/redo lifecycle | [widgetHistory.ts](../src/store/widgetHistory.ts) | [boardHydration.test.ts](../src/store/boardHydration.test.ts), [widgetHistory.test.ts](../src/store/widgetHistory.test.ts) |

## High-cost files: search before reading

| File | Search first for | Why it is expensive |
|---|---|---|
| [fields.ts](../src/widgets/fields.ts) | Widget type, field name, command name | Thin composition barrel; descriptors live in the per-domain files under [fields/](../src/widgets/fields) (`coreWidgetFields`, `dataMediaFields`, `studyFields`, `inputLogicFields`, `professionalFields`, `coreCommands`, `valueHelpers`) |
| [registry.ts](../src/widgets/registry.ts) | Widget type or `WidgetDefinition` | Composition barrel; definitions live in the per-domain files under [registry/](../src/widgets/registry) — spread order there IS registry key order, never reorder |
| [EssentialWidgets.tsx](../src/components/widgets/modules/EssentialWidgets.tsx) | Exported widget component name | Re-export barrel; renderers live under [modules/essential/](../src/components/widgets/modules/essential) (`inputWidgets`, `workflowWidgets`, `analysisWidgets`, `opsWidgets`, shared helpers) and still ship as one lazy chunk |
| [renderers](../src/components/widgets/renderers) | Widget type or family id | Family maps own dispatch and lazy loaders without expanding the central shell |
| [WidgetCard.tsx](../src/components/widgets/WidgetCard.tsx) | Interaction name, data attribute, or child component | Shell gestures, ports, and chrome meet here; outline-edge resize lives in [useWidgetResize.ts](../src/components/widgets/useWidgetResize.ts), content-floor measurement in [useContentFloor.ts](../src/components/widgets/useContentFloor.ts) |
| [CanvasViewport.tsx](../src/components/canvas/CanvasViewport.tsx) | Overlay/component name or shortcut | Composition root imports most application surfaces |
| [scenarioResolver.ts](../src/utils/scenarioResolver.ts) | Scenario/archetype name or exported resolver | Classification, routing, preferences, and resolution share one file |
| [product.css](../src/styles/product.css) | Stable `gp-` class or `data-*` selector — search the [product/](../src/styles/product) partials | Ordered `@import` manifest; the cascade spans the six numbered partials, whose order is load-bearing |

## Recurring route: canvas line work

Start with the semantic layer named by the user. Relation, dependency, and wire visuals share [CanvasEdge.tsx](../src/components/canvas/CanvasEdge.tsx) and the `Shared canvas edge paint system` section of [index.css](../src/index.css). Their endpoint geometry and state models remain separate. Inspect [curve.ts](../src/utils/curve.ts), [dependencyGeometry.ts](../src/utils/dependencyGeometry.ts), or [portGeometry.ts](../src/utils/portGeometry.ts) only after identifying the owning semantic layer.

At minimum verify finite SVG geometry, zoom alignment, endpoint side, selection hit target, deletion, and no `NaN`/`Infinity` attributes.

## Recurring route: fullscreen widget editors

A widget whose real interaction surface can't share the app canvas's wheel/pointer
input (a drawing/diagramming tool, anything embedding a heavy third-party editor)
follows the pattern in [modules/excalidraw/](../src/components/widgets/modules/excalidraw):
the in-card renderer is a non-interactive preview/launcher only; the actual editor
mounts exclusively inside a `createPortal(..., document.body)` overlay modeled on
[ConfirmDialog.tsx](../src/components/ui/ConfirmDialog.tsx) (`useOverlayLifecycle`,
`useFocusTrap`, Escape-to-close, `z-[200]` tier — see the z-index scale used across
`src/components`). Because the portal renders outside the canvas viewport's DOM
subtree, none of [gestureEngine.ts](../src/engine/camera/gestureEngine.ts)'s native
wheel/pointerdown listeners (bound unconditionally on the viewport root) ever see
its input — no `stopPropagation` gymnastics needed. Debounce persistence writes
(`createOwnedTimeout` in [ownedTimeout.ts](../src/utils/ownedTimeout.ts)) rather
than calling `onUpdate` on every change event from a high-frequency editor. A
heavy third-party dependency stays out of the initial bundle by living only
behind the widget's own `lazy()` entry in the matching `renderers/lazy*.ts` file —
no separate manual `import()` wrapper is needed once that boundary exists. Large
embedded binary content (pasted images) never lands in board JSON — write it to
[boardDatabase.ts](../src/utils/boardDatabase.ts)'s media blob store and persist
only a lightweight reference, mirroring `MediaData.localBlobKey`.

## Code review route

1. `git diff --stat` and `git diff --name-only`.
2. Read changed hunks before full files.
3. Find direct callers/importers with `rg -n "from ['\"]<module>|<symbol>" src`.
4. Map each changed behavior to its owner and invariant above.
5. Review interrupted gestures, unmounts, deletion, empty data, reload, reduced motion, and far zoom where relevant.
6. Run the smallest proving test, then expand according to blast radius.

## Verification routes

| Scope | Required command / check |
|---|---|
| Navigation docs only | `npm run docs:check` |
| One pure helper | `npm test -- <nearest-test-file>` plus typecheck and lint |
| Store, persistence, shared types, registry, fields, routing | `npm run check` |
| Production bundle or global CSS | `npm run check:full` plus applicable [manual smoke checklist](manual-smoke-checklist.md) items |

## Keeping the map trustworthy

Update this file in the same commit when:

- An entrypoint moves or is renamed.
- State or contract ownership changes.
- A new recurring subsystem or verification command is introduced.
- A route repeatedly requires opening an unlisted file.

Do not paste implementation into this map. Keep routes, symbols, contracts, and verification only. `npm run docs:check` must remain green.
