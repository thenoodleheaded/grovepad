# Grovepad codebase map

This is the compact navigation layer for fresh AI tasks. It routes user language to the smallest useful source surface. For system diagrams, fan-in/fan-out, cycles, and deep invariants, continue to the [architecture map](architecture-map.md).

## How to use this map

1. Search task words here before opening source: `rg -n -i "panel|dependency|quick add" docs/codebase-map.md`.
2. Start with the first file in the matching route.
3. Search the listed symbols/classes inside those files and read narrow ranges.
4. Inspect direct consumers only when the requested behavior crosses the stated owner.
5. Run the route's targeted check before escalating to the full suite or browser.

Paths and headings are checked by `npm run docs:check`. Use symbol names rather than line numbers because lines drift.

## Task router

| User language / task | Start here | State or contract owner | Styles / UX law | Nearest verification |
|---|---|---|---|---|
| App boot, login, guest route, staying signed in across updates | [App.tsx](../src/App.tsx), [LoginPage.tsx](../src/components/auth/LoginPage.tsx) | [useAuthStore.ts](../src/store/useAuthStore.ts) — also owns `deleteAccount` (Settings → Account, calls `public.delete_own_account()`; server confirms before any local data is cleared, so a failed deletion never costs somebody their boards) and `rememberedAccount`, the last signed-in identity that keeps an OFFLINE boot off the login page (only `signOut` forgets it) | Session durability: [durableAuthStorage.ts](../src/lib/durableAuthStorage.ts) writes the session to localStorage AND an IndexedDB mirror under an app-owned key, so a wiped localStorage or a changed project reference no longer signs anybody out; the client options that depend on it are spelled out in [supabase.ts](../src/lib/supabase.ts). Styling: [index.css](../src/index.css) | Build plus guest/signed-in smoke, and a signed-in reload with localStorage cleared |
| Billing, Polar webhooks, checkout, subscription reconciliation | [routes.ts](../worker/billing/routes.ts) (`/api/billing/webhook`, `/api/billing/checkout`); entry point [billingWorker.ts](../worker/billingWorker.ts), deployed separately by `npm run deploy:billing` from [wrangler.billing.toml](../wrangler.billing.toml) | Signature verification, the only thing between a stranger and a free subscription: [standardWebhooks.ts](../worker/billing/standardWebhooks.ts); pure payload→row mapping: [polarEvents.ts](../worker/billing/polarEvents.ts); the only writer of `public.subscriptions`: [subscriptionStore.ts](../worker/billing/subscriptionStore.ts); nightly repair for missed webhooks: [reconcile.ts](../worker/billing/reconcile.ts); idempotency table: [20260812100000_billing_events.sql](../supabase/migrations/20260812100000_billing_events.sql) | Identity is never taken from the request body — the webhook derives it from a signature, checkout from a Supabase token. `SUPABASE_SERVICE_ROLE_KEY` lives ONLY in this Worker, never in the Canvas relay and never in the browser. Grace/retention constants are mirrored from [entitlements.ts](../src/subscription/entitlements.ts) | [standardWebhooks.test.ts](../worker/billing/standardWebhooks.test.ts), [polarEvents.test.ts](../worker/billing/polarEvents.test.ts), [routes.test.ts](../worker/billing/routes.test.ts), [reconcile.test.ts](../worker/billing/reconcile.test.ts), then a Polar sandbox lifecycle pass |
| Account deletion side effects: Sign in with Apple token revocation (`/api/account/apple/*`) | [appleRevoke.ts](../worker/account/appleRevoke.ts); entry point [accountWorker.ts](../worker/accountWorker.ts), deployed from [wrangler.account.toml](../wrangler.account.toml) | Called by `deleteAccount` in [useAuthStore.ts](../src/store/useAuthStore.ts) before `delete_own_account()`. Holds the Apple .p8 key, never the service_role key; stores no Apple tokens. Runbook: [apple-sign-in.md](apple-sign-in.md) |  |  |
| Subscription, entitlements, Air vs free, trial, lapse, plan gating | [entitlements.ts](../src/subscription/entitlements.ts) is the ONE owner of "what does this account get" — never re-derive `isSubscribed` anywhere else | Account-scoped record and offline-grace cache: [useSubscriptionStore.ts](../src/store/useSubscriptionStore.ts); auth wiring and revalidation: [subscriptionRuntime.ts](../src/runtime/subscriptionRuntime.ts); server mirror of the same verdict (`public.has_air_entitlement`, grace window duplicated as `interval '7 days'`): [20260812090000_subscriptions.sql](../supabase/migrations/20260812090000_subscriptions.sql) | Nothing local is ever gated: local boards, widgets, skins and JSON/Markdown export stay free on every plan and after a lapse. Plan and prices: [subscription-plan.md](subscription-plan.md); public copy: [terms.html](../public/terms.html) | [entitlements.test.ts](../src/subscription/entitlements.test.ts), [useSubscriptionStore.test.ts](../src/store/useSubscriptionStore.test.ts), then `npm run check` |
| Usage counting, analytics, PostHog, "how many people opened it", opt-out | [analytics.ts](../src/services/analytics.ts) | One event exists, `app_opened`, sent once per app start by [analyticsRuntime.ts](../src/runtime/analyticsRuntime.ts); consent is the `usageAnalytics` preference in [useSettingsStore.ts](../src/store/useSettingsStore.ts), and off means the SDK is never imported | Adding a second event is a privacy change: the events are enumerated by name in [privacy.html](../public/privacy.html) and [quit-rules.md](quit-rules.md), and all three move together. Connect-src for `https://*.i.posthog.com` lives in [_headers](../public/_headers) and [tauri.conf.json](../src-tauri/tauri.conf.json) | [analytics.test.ts](../src/services/analytics.test.ts), [analyticsRuntime.test.ts](../src/runtime/analyticsRuntime.test.ts), then one real `app_opened` in PostHog → Activity |
| Canvas composition, global shortcuts, overlay mounting | [CanvasViewport.tsx](../src/components/canvas/CanvasViewport.tsx) | Runtime start/stop: [appRuntime.ts](../src/runtime/appRuntime.ts); canvas/store owners below | [product.css](../src/styles/product.css) | Build, lint, applicable smoke |
| Pan, zoom, camera history, pointer selection | Camera core: [cameraEngine.ts](../src/engine/camera/cameraEngine.ts); gestures: [gestureEngine.ts](../src/engine/camera/gestureEngine.ts); public seam: [useCanvasStore.ts](../src/store/useCanvasStore.ts) | Gesture decision table: [canvasGesturePolicy.ts](../src/utils/canvasGesturePolicy.ts); marquee merge rules (plain box replaces, ⇧ adds, ⌥ subtracts; the modifier is read once at pointerdown): [marqueeSelection.ts](../src/utils/marqueeSelection.ts); fling math: [glidePhysics.ts](../src/engine/camera/glidePhysics.ts); content-anchored world rect for SVG layers (deliberately camera-independent, so panning never re-lays-out an edge layer): [useWorldContentRect.ts](../src/hooks/useWorldContentRect.ts); [canvasView.ts](../src/utils/canvasView.ts) | Contract: [canvas engine](canvas-engine.md) | Engine/physics/policy tests, typecheck, then pan/zoom/select smoke |
| Adaptive input modality, interaction modes, viewport class, safe-area chrome | [adaptiveInputRuntime.ts](../src/runtime/adaptiveInputRuntime.ts), [useAdaptiveInputStore.ts](../src/store/useAdaptiveInputStore.ts) | Pure capability rules: [adaptiveInput.ts](../src/utils/adaptiveInput.ts); compact chrome decisions (minimap, tab row, touch chrome, dock history, Select more): [adaptiveChrome.ts](../src/utils/adaptiveChrome.ts); touch tool dock (Navigate/Select, plus Undo/Redo on phone and tablet; never a second Circuit toggle): [CanvasModeDock.tsx](../src/components/ui/CanvasModeDock.tsx), shown by the `.gp-canvas-mode-dock` CSS rule; safe-area/target tokens: [01-tokens-base.css](../src/styles/product/01-tokens-base.css) | Capability detection uses pointer/media/visual viewport signals, never user-agent device guesses | Closest adaptive-input/chrome test, typecheck, then responsive browser smoke |
| Responsive canvas toolbar, workspace menu, canvas tree, accessible spatial outline, search, capture, imports, minimap, zoom, controls reference | [CanvasToolbar.tsx](../src/components/ui/CanvasToolbar.tsx), then the named surface in [components/ui](../src/components/ui); the controls reference is embedded in [SettingsPanel.tsx](../src/components/ui/SettingsPanel.tsx) | Legal canvas moves: [canvasTreePolicy.ts](../src/utils/canvasTreePolicy.ts); semantic tree order/navigation: [canvasOutline.ts](../src/utils/canvasOutline.ts); adaptive chrome: [adaptiveChrome.ts](../src/utils/adaptiveChrome.ts); overlay state: [uiLinkingSlice.ts](../src/store/slices/uiLinkingSlice.ts) | Follow the [popup panel design standard](popup-panel-design.md) for temporary panels; search `gp-command`, `gp-widget-picker`, `gp-quick-add`, `gp-import`, `gp-settings`, and safe-area classes in [product.css](../src/styles/product.css) | Policy/outline/contract tests, typecheck, lint, then keyboard-traverse and open/close every overlay in browser |
| Canvas tabs: open canvases, tab switching, close/reorder, per-device tab row | Row UI: [CanvasTabs.tsx](../src/components/ui/CanvasTabs.tsx); actions: [navigationSlice.ts](../src/store/slices/navigationSlice.ts) | Pure tab model and the repair every canvas-removing write must run: [canvasTabs.ts](../src/store/canvasTabs.ts); navigate-vs-new-tab intent shared by canvas cards, tree, breadcrumbs: [canvasOpenIntent.ts](../src/utils/canvasOpenIntent.ts); device persistence: [persistedDeviceState.ts](../src/utils/persistedDeviceState.ts); row visibility: [adaptiveChrome.ts](../src/utils/adaptiveChrome.ts) | Tabs are device-local and never enter a board document; the active tab always points at `activeCanvasId`; `resolveCanvasTabs` runs after undo/redo, board load, workspace delete, and the canvas-node delete cascade. Styles: search `gp-canvas-tab` in [product.css](../src/styles/product.css) | [canvasTabs.test.ts](../src/store/canvasTabs.test.ts), [canvasTabStore.test.ts](../src/store/canvasTabStore.test.ts), [persistedDeviceState.test.ts](../src/utils/persistedDeviceState.test.ts), then open/close/switch/reorder tabs and reload in browser |
| Widget shell drag, resize, icon scaling, selection, ports, magnetic hover | [WidgetCard.tsx](../src/components/widgets/WidgetCard.tsx), [useWidgetResize.ts](../src/components/widgets/useWidgetResize.ts), [useWidgetMagneticHover.ts](../src/components/widgets/useWidgetMagneticHover.ts) | Input arbitration (`isInteractiveWidgetTarget` must stay `Element`-wide, or icon-only controls read as drag surface): [widgetPointerPolicy.ts](../src/utils/widgetPointerPolicy.ts); outline-proximity hit geometry: [widgetResizeEdge.ts](../src/utils/widgetResizeEdge.ts); icon scaling (a square dragged continuously across `ICON_MIN_EDGE`–`ICON_MAX_EDGE`, then snapped on release to a 2×2 or 3×3 grid square without intermediate size states; helpers `clampIconEdge`/`snapIconEdgeToGrid`/`crushesToIcon`/`iconEscapesToFull` in [widgetScale.ts](../src/utils/widgetScale.ts)) and `sizing.fixed` in [spatial.ts](../src/types/spatial.ts) / [contracts/registry.ts](../src/widgets/contracts/registry.ts); [useWidgetStore.ts](../src/store/useWidgetStore.ts); bounded pointer geometry: [widgetMagnetism.ts](../src/utils/widgetMagnetism.ts); glue bond/seam geometry: [glueGeometry.ts](../src/utils/glueGeometry.ts); align/distribute unit geometry (families move as blocks): [widgetAlignment.ts](../src/utils/widgetAlignment.ts); shared copy/cut/paste clipboard: [widgetClipboard.ts](../src/utils/widgetClipboard.ts) | Search `gp-widget-card`, `gp-resize-edge`, `data-resize-edge`, `data-elastic`, `data-unglue-intent`, and `data-magnetic-hover` in [index.css](../src/index.css) and [product.css](../src/styles/product.css) | Closest pointer/glue tests, build, plus mouse/touch/Pencil drag-resize-cancel smoke |
| Drawing widget; Quick Ink, Whiteboard, Graph Paper, Dot Grid, Storyboard, Annotation, Excalidraw; Apple Pencil, palm rejection, hover preview, image/PDF markup | [DrawingWidget.tsx](../src/components/widgets/modules/DrawingWidget.tsx) dispatches all seven modes; native pressure ink stays in [SketchpadWidget.tsx](../src/components/widgets/modules/SketchpadWidget.tsx), while the heavy editor stays lazy/fullscreen in [modules/excalidraw/](../src/components/widgets/modules/excalidraw) | `SketchpadData` keeps shared native strokes, independent storyboard/annotation `skinStates`, and an independent Excalidraw scene; validation/defaults: [drawingSkinModel.ts](../src/components/widgets/modules/drawingSkinModel.ts); pure sampling/width/simplification/eraser policy: [sketchpadStroke.ts](../src/utils/sketchpadStroke.ts). The standalone `excalidraw` card is deleted; a board saved with one has that record DROPPED on load (see [deletedWidgetTypes.ts](../src/widgets/deletedWidgetTypes.ts)), and recipes emit the skin form via [build-recipes.py](../scripts/build-recipes.py) | Purpose-built studio surfaces: [19-drawing-skins.css](../src/styles/product/19-drawing-skins.css); Article XIII in [widget constitution](widget-constitution.md); search `gp-sketch`, `data-pencil-drawing`, and Scribble comments in [product.css](../src/styles/product.css) | [drawingSkinModel.test.ts](../src/components/widgets/modules/drawingSkinModel.test.ts), [DrawingWidget.test.tsx](../src/components/widgets/modules/DrawingWidget.test.tsx), [drawingSkins.test.ts](../src/widgets/drawingSkins.test.ts), stroke/pointer/history contracts, then exercise draw/erase/undo/redo, frame add/reorder/delete, reference import/fade, Excalidraw open/edit/close, resize, both themes, and reduced motion |
| Settings, keyboard and gesture reference, device-local UI preferences | [SettingsPanel.tsx](../src/components/ui/SettingsPanel.tsx), [ShortcutsOverlay.tsx](../src/components/ui/ShortcutsOverlay.tsx) | [useSettingsStore.ts](../src/store/useSettingsStore.ts); theme state: [useThemeStore.ts](../src/store/useThemeStore.ts); browser zoom and viewport breakpoints own interface scale | [Popup panel design standard](popup-panel-design.md); `.gp-settings-*` and preference-driven `data-*` rules in product styles | Settings contract test, typecheck, lint, then desktop/tablet/phone settings smoke |
| Visual quality modes (Full / Balanced / Light), blur budget, tier-scaled aura | Picker: [SettingsPanel.tsx](../src/components/ui/SettingsPanel.tsx) (`VisualQualityIsland`); tier stylesheet: [35-visual-quality.css](../src/styles/product/35-visual-quality.css) | `visualQuality` lives in [useSettingsStore.ts](../src/store/useSettingsStore.ts), which publishes `html[data-quality]` and treats `low` as reduced motion without overwriting the separate motion preference. Aura budget per tier (`AURA_QUALITY_BUDGET`, `auraTuningForQuality`, `auraBufferSize(…, qualityScale)`): [auraTuning.ts](../src/components/canvas/auraTuning.ts); `low` unmounts [CanvasAuraLayer.tsx](../src/components/canvas/CanvasAuraLayer.tsx) entirely rather than dimming it | `high` is the stylesheet as authored — there is deliberately NO `[data-quality='high']` block, so the default look cannot drift. Every backdrop blur is declared as `blur(calc(Npx * var(--gp-blur-scale, 1)))`; a bare radius would ignore the tier. The tier partial loads LAST in [product.css](../src/styles/product.css) and only ever removes work (blur, glow shadow layers, gradient stacks, animation) | [visualQuality.test.ts](../src/store/visualQuality.test.ts), settings contract test, typecheck, lint, then switch all three tiers in both themes and check card selection, expand halo, and panel legibility |
| Whole-card content-safe sizing, grow-only adjustment, icon state | [widgetContentFloor.ts](../src/utils/widgetContentFloor.ts), [WidgetCard.tsx](../src/components/widgets/WidgetCard.tsx) | Registry fallback windows: widget registry; ephemeral mounted floors: [liveWidgetSizing.ts](../src/store/liveWidgetSizing.ts); clamp/state: [widgetLayoutSlice.ts](../src/store/slices/widgetLayoutSlice.ts) | Article XII.1 in [widget constitution](widget-constitution.md), Article XVIII in [glass constitution](widget-glass-constitution.md) | content-floor, scale-state, and sizing-profile tests; `npm run check:full`; narrow/wide/icon smoke |
| Widget contents, one specific widget, or a widget mode family | Search the type in the matching family map under [renderers](../src/components/widgets/renderers), then open the named file in [modules](../src/components/widgets/modules); consolidated mode adapters live in [consolidatedWidgetRenderers.tsx](../src/components/widgets/renderers/consolidatedWidgetRenderers.tsx) | Family maps own typed dispatch; [WidgetRenderer.tsx](../src/components/widgets/WidgetRenderer.tsx) owns suspense, the forward-compatibility placeholder, and the responsive shell. Cards that became skins of another widget are DELETED, not hidden: they are absent from `ModuleType`, from `WIDGET_REGISTRY`, and from every field/renderer table, so nothing can create or look one up. [deletedWidgetTypes.ts](../src/widgets/deletedWidgetTypes.ts) is the one list of their names — the notes/chart/time/drawing families plus `kanban`/`assignment`/`daily_agenda`/`weekly_planner`/`timeline`/`priority_matrix`, `vocab`/`quiz`, `study_goal`/`okr`/`progress`, `gpa`, `random_picker`, and the long-gone `divider` — and it imports nothing at all, so the registry can read it while assembling. Deletion is total: [persistedBoardSchema.ts](../src/utils/persistedBoardSchema.ts) DROPS those records on both hydration paths rather than converting them, so a card cannot return to a canvas that once held it, and the relation/wire/glue parsers then discard every edge that named it. Nothing migrates the old content — a board saved with one of these cards loses what was inside it, by design. [build-recipes.py](../scripts/build-recipes.py) emits the skin form so recipes only place live types, and [thoughtInterpreter.ts](../src/utils/thoughtInterpreter.ts) keeps `LEGACY_WIDGET_PHRASES` so a person who still says "kanban" lands on the live widget that does that job. Two deleted names survive as renderer identities only (`random_picker`, `okr` — see `SkinRendererType` in [contracts/fields.ts](../src/widgets/contracts/fields.ts)) because the generic expansion renderer still draws Decision's Weighted and Goal's OKR skins under them | Both constitutions; shared island vocabulary in [WidgetPanel.tsx](../src/components/widgets/WidgetPanel.tsx) | Renderer-family ownership and widget-consolidation tests, closest widget test, then build |
| Widget skins (the alternate shapes one card wears), the title-anchored skin roller, per-skin accents, haptic detents | [WidgetSkinRoller.tsx](../src/components/widgets/WidgetSkinRoller.tsx), opened by the title-capsule icon in [WidgetCard.tsx](../src/components/widgets/WidgetCard.tsx) via [useWidgetSkinSwitch.ts](../src/components/widgets/useWidgetSkinSwitch.ts) | One owner answers what skins a card has, which it wears, what data wearing another produces, and what colour it takes: [widgetSkins.ts](../src/utils/widgetSkins.ts). The broad opportunity list lives in [widget-skin-opportunity-catalogue.md](widget-skin-opportunity-catalogue.md); the stricter skin/control/preset/widget ownership ledger lives in [widget-skin-reorganization.md](widget-skin-reorganization.md). [build-widget-skin-reorganization.py](../scripts/build-widget-skin-reorganization.py) rebuilds that planning ledger and [skinOwnership.generated.ts](../src/widgets/skinOwnership.generated.ts); these labels document future ownership but never hide existing catalogue choices. [build-widget-skin-blueprints.py](../scripts/build-widget-skin-blueprints.py) generates [skinBlueprints.generated.ts](../src/widgets/skinBlueprints.generated.ts), and [skinCatalog.ts](../src/widgets/skinCatalog.ts) installs the complete catalogue. Hand-authored registry skins stay first. Existing consolidated skins persist in `data.mode`, while newly skinned widgets persist appearance in `data.skin` so a domain-owned `mode` can never be overwritten. Optional advanced-mode data is isolated under `data.skinStates`. Drum physics, detents, wheel stepping, end resistance, barrel placement: [skinRollerGeometry.ts](../src/components/widgets/skinRollerGeometry.ts) — `ANGLE_STEP` is the drum's legibility budget, not just its shape: a row rotated by `a` is projected to `cos(a)` of its height, and while the drum rolls fast every row is mid-rotation, so the step is held flat enough that the three choices each way the sharp zone promises are actually readable. `ANGLE_STEP`, `DRUM_RADIUS`, `PROJECTION_SCALE`, and the derived `DRUM_PERSPECTIVE` are ONE tuning — the component must never set a perspective of its own; every physical tick goes through [haptics.ts](../src/utils/haptics.ts) — the one seam that will drive phone vibration | Search `gp-skin-roller` in [02-canvas-toolbar.css](../src/styles/product/02-canvas-toolbar.css); shared renderer arrangements live in [08-skin-presentations.css](../src/styles/product/08-skin-presentations.css); `gp-skin-roller-behind` is the blur applied to the app root while the drum is open | [skinCatalog.test.ts](../src/widgets/skinCatalog.test.ts), [skinRollerGeometry.test.ts](../src/components/widgets/skinRollerGeometry.test.ts), then roll by drag, wheel, and arrow keys on short lists and confirm Esc leaves the skin unchanged |
| Text skins: Plain, Sticky, Typewriter | [TextWidget.tsx](../src/components/widgets/modules/TextWidget.tsx), [StickyNoteWidget.tsx](../src/components/widgets/modules/StickyNoteWidget.tsx), selected by the `text` branch in [consolidatedWidgetRenderers.tsx](../src/components/widgets/renderers/consolidatedWidgetRenderers.tsx) | Shared writing remains in `TextData.text`; the Typewriter focus preference lives in an isolated `skinStates` pocket. Bounded Sticky ink validation lives in [textSkinModel.ts](../src/components/widgets/modules/textSkinModel.ts). Sticky is a colour and nothing else — no ruling, no tape — plus a one-nib pen whose strokes are 0–1 surface fractions in the skin's own `skinStates.sticky` pocket, so a scribble holds its place over the words at every card size. No skin shows a word count; the footer is the collaborator line alone. Cornell Notes, Markdown Page, Callout, Quote, Daily Log and Versioned Note were all removed: a journal belongs to [Logbook](../src/components/widgets/modules/LogbookWidget.tsx), whose Daily Log skin keeps real timestamps, day grouping and levels, and a board saved wearing any removed Text skin keeps every word of its `text` and falls back to Plain. The card itself was renamed from Note to Text on 2026-08-13; [renamedWidgetTypes.ts](../src/widgets/renamedWidgetTypes.ts) rewrites a persisted `notes` record to `text` on hydration before anything else inspects it, so an old board reads back under the live name instead of falling into the opaque-placeholder path. The standalone `quote`, `sticky_note` and `cornell` cards are all deleted outright; a board saved with one has that record dropped on hydration (see [deletedWidgetTypes.ts](../src/widgets/deletedWidgetTypes.ts)), so it returns neither as Text nor as a placeholder. Native companion snapshots intentionally reduce every advanced mode to Plain while preserving the text. All three skins are declared in [structureNotesWidgets.ts](../src/widgets/registry/structureNotesWidgets.ts) so each wears its own icon rather than the catalogue's per-presentation fallback. The resting tile is the OPEN card photographed rather than a summary: [TextRestPage.tsx](../src/components/widgets/modules/TextRestPage.tsx) draws a hook-free, non-editable mirror of the worn skin at the card's true width, `NoteFace` in [WidgetRestingFace.tsx](../src/components/widgets/WidgetRestingFace.tsx) scales it by the one ratio the tile was measured from, and `RestNoteModel` therefore carries no preview text at all — which is what makes "nothing is hidden at rest" true by construction rather than by line limits | [09-note-skins.css](../src/styles/product/09-note-skins.css), following the one-backplate and lone-text-control laws in [widget-glass-constitution.md](widget-glass-constitution.md): every wrapper holding a note's textarea carries `gp-bare-field`, or the auto field-island detector in [06-field-islands.css](../src/styles/product/06-field-islands.css) manufactures a second glass pane inside the card. Two rules govern the file: sizes are a RATIO (the skin root takes its font size from the card's width in `cqi`, everything inside is `em`, and hairlines/pointer targets stay px), and no colour is written twice — accents mix toward `--gp-note-lift`, which is white on dark glass and black on paper, because a literal `white` mix is what made every eyebrow wash out in the light theme. Sticky's scale band is deliberately narrower than the rest. The Sticky tint bleeds past its own box to reach the card edge, so the note reports `offsetHeight`, never `scrollHeight`, which counts that bleed | [textSkinModel.test.ts](../src/components/widgets/modules/textSkinModel.test.ts), [TextWidget.test.tsx](../src/components/widgets/modules/TextWidget.test.tsx), [textSkins.test.ts](../src/widgets/textSkins.test.ts), [restingFace.test.ts](../src/utils/restingFace.test.ts), [WidgetRestingFace.test.tsx](../src/components/widgets/WidgetRestingFace.test.tsx), then exercise every skin, the Sticky pen and eraser, the Typewriter focus toggle, resizing a card to watch its type scale with it, narrow widths, light/dark, keyboard focus, and reduced motion |
| Calculator skins: Basic, Scientific, Tape, Finance, Programmer, Date Math, Named Variables | [CalculatorWidget.tsx](../src/components/widgets/modules/CalculatorWidget.tsx), dispatched by the `calculator` branch in [coreWidgetRenderers.tsx](../src/components/widgets/renderers/coreWidgetRenderers.tsx) | `CalculatorData.result` is the circuit's one canonical output and `expression` the readable record of how it was reached — every skin writes both. All the arithmetic is pure and in [calculatorSkinModel.ts](../src/components/widgets/modules/calculatorSkinModel.ts): two hand-written parsers (`^` is power in the general one, XOR in the integer one), no `eval`/`Function` anywhere, and every input bounded on length and nesting because a persisted expression is untrusted. The tape's rows, the finance inputs, the base, the dates, and the named values live in isolated `skinStates`. Named Variables is fed by three writable ports (`variable_1..3` in [dataMediaFields.ts](../src/widgets/fields/dataMediaFields.ts)) that re-evaluate `result` in the same write; `result` stays field slot 0, so existing wires keep their port. Date Math and Named Variables are `rendererOwnedSkinDetails`, and `skinField` stays `'skin'` because `CalculatorData` has no `mode` | [20-calculator-skins.css](../src/styles/product/20-calculator-skins.css): the readout is a genuine well (a displayed value), keys and rows sit on the one backplate, and every wrapper holding a text control carries `gp-bare-field` | [calculatorSkinModel.test.ts](../src/components/widgets/modules/calculatorSkinModel.test.ts), [CalculatorWidget.test.tsx](../src/components/widgets/modules/CalculatorWidget.test.tsx), [calculatorSkins.test.ts](../src/widgets/calculatorSkins.test.ts), then exercise every skin, a wire write into a named value, keyboard entry and Enter, narrow cards, and reduced motion |
| Toggle skins: Switch, Checkbox, Power, Segment, Availability, Tri-state | [ToggleWidget.tsx](../src/components/widgets/modules/ToggleWidget.tsx), dispatched by the `toggle` branch in [workflowWidgetRenderers.tsx](../src/components/widgets/renderers/workflowWidgetRenderers.tsx) and re-exported through [EssentialWidgets.tsx](../src/components/widgets/modules/EssentialWidgets.tsx) so it stays in the same lazy chunk | `ToggleData.value` is the circuit's one canonical boolean — every skin reads and writes exactly it, and every skin keeps it on screen. The segment's two choice names and the tri-state's third position live in isolated `skinStates`, and the boolean overrules a stale third position ([toggleSkinModel.ts](../src/components/widgets/modules/toggleSkinModel.ts), which also owns native-style arrow/Home/End radio navigation and gives [restingFace.ts](../src/utils/restingFace.ts) the skin's own word so a folded Availability card rests as "Busy"). Tri-state is `rendererOwnedSkinDetails`, and `skinField` stays `'skin'` because `ToggleData` has no `mode` | [17-toggle-skins.css](../src/styles/product/17-toggle-skins.css): controls are shaped surfaces on the one backplate, the label wrapper carries `gp-bare-field`, and the `dashboard` presentation's accent recolouring of `<strong>` is undone so Availability keeps its status hue | [toggleSkinModel.test.ts](../src/components/widgets/modules/toggleSkinModel.test.ts), [ToggleWidget.test.tsx](../src/components/widgets/modules/ToggleWidget.test.tsx), [toggleSkins.test.ts](../src/widgets/toggleSkins.test.ts), then exercise every skin, keyboard operation of each control, a wire write while a skin is worn, short cards, and reduced motion |
| Rating skins: Stars, Slider, Emoji, Traffic Light, NPS, Rubric, Confidence | [RatingWidget.tsx](../src/components/widgets/modules/RatingWidget.tsx), dispatched by the `rating` branch in [coreWidgetRenderers.tsx](../src/components/widgets/renderers/coreWidgetRenderers.tsx) | `RatingData.value` remains the one canonical 0–5 rating, so wires and Clear work identically in every presentation. NPS maps that value reversibly onto 0–10; traffic and emoji are discrete readings of it; Rubric publishes the mean whenever a criterion moves; Confidence pairs it with a bounded certainty percentage and evidence note. Rubric criteria and confidence evidence remain isolated in `skinStates`, both specialist skins own their controls through `rendererOwnedSkinDetails`, and old unskinned cards fall back to Stars. [ratingSkinModel.ts](../src/components/widgets/modules/ratingSkinModel.ts) owns every clamp, conversion, label, and persisted-state boundary; [restingFace.ts](../src/utils/restingFace.ts) keeps the worn presentation when the card folds | [27-rating-skins.css](../src/styles/product/27-rating-skins.css): stars, faces, signals, scales, and the confidence ring are shaped controls on the card's one backplate; text wrappers carry `gp-bare-field`, equal choices remain pixel-identical, and reduced motion removes all flourish without removing feedback | [ratingSkinModel.test.ts](../src/components/widgets/modules/ratingSkinModel.test.ts), [RatingWidget.test.tsx](../src/components/widgets/modules/RatingWidget.test.tsx), [ratingSkins.test.ts](../src/widgets/ratingSkins.test.ts), then exercise all seven skins, arrow/Home/End movement, wire writes, narrow cards, both themes, folded faces, and reduced motion |
| Formula skins: Two-input, Percent Change, Ratio, Growth, Expression, Weighted Score, Conditional | [FormulaWidget.tsx](../src/components/widgets/modules/FormulaWidget.tsx), dispatched by the `formula` branch in [workflowWidgetRenderers.tsx](../src/components/widgets/renderers/workflowWidgetRenderers.tsx) and re-exported through [EssentialWidgets.tsx](../src/components/widgets/modules/EssentialWidgets.tsx) so it stays in the same lazy chunk | The card holds up to SIX numbers — `a` … `f` — and every one of them is a circuit port in [inputLogicFields.ts](../src/widgets/fields/inputLogicFields.ts). `a` and `b` stay canonical and `c`–`f` are appended AFTER `result` because a field's index IS its port slot, so no existing board's rail moves; a wire writing `d` opens that slot on the card, because a number a reader cannot see is a number the card is hiding. Slots carry the card's own names (`names`), and a written expression may call any of them by name or by letter. A skin changes the QUESTION asked of those numbers and picks WHICH of them fill its roles (`fromKey`/`toKey`, `partKey`, `startKey`/`rateKey`, `leftKey`/`rightKey` in `skinStates`, falling back when a stored key is no longer held), so [formulaSkinModel.ts](../src/components/widgets/modules/formulaSkinModel.ts) owns the single answer — `formulaReading` feeds the renderer's hero, the resting tile, and the `result` field alike, and the card's `unit`/`precision` round the PUBLISHED number too, so what a reader sees and what a wire carries can never differ. A card with no stored `skin` and no extra slots answers exactly as the original two-operand card did. Expression, weighted rows, growth periods and the two conditional branches (either a number or an expression) live in isolated `skinStates`, and the three schema-extension skins own their controls through `rendererOwnedSkinDetails`. `skinField` stays `'skin'` because `FormulaData` has no `mode`. The evaluator is the Calculator's — see [calculatorSkinModel.ts](../src/components/widgets/modules/calculatorSkinModel.ts), which now parses `%`, comparisons answering 1/0, `&&`/`||`, multi-argument `min`/`max`/`sum`/`avg`/`pow`/`clamp`/`round`/`log`, and an `if` that picks its branch before evaluating it so `if(b = 0, 0, a / b)` is safe | [25-formula-skins.css](../src/styles/product/25-formula-skins.css): every slot in the rack is pixel-identical (Article XVIII) and so are the two conditional branches, the answer is the one hero in a genuine well, and every control wrapper carries `gp-bare-field` | [formulaSkinModel.test.ts](../src/components/widgets/modules/formulaSkinModel.test.ts), [FormulaWidget.test.tsx](../src/components/widgets/modules/FormulaWidget.test.tsx), [formulaSkins.test.ts](../src/widgets/formulaSkins.test.ts), then exercise all seven skins, growing and shrinking the rack, a wire write into an unopened slot, a zero divisor mid-chain, a broken expression, narrow cards, and reduced motion |
| Formula Sheet skins: Reference Sheet, Equation Cards, Exam Strip, Derivation, Unit-aware, Worked Example | [FormulaSheetWidget.tsx](../src/components/widgets/modules/FormulaSheetWidget.tsx), dispatched by the `formula_sheet` branch in [educationWidgetRenderers.tsx](../src/components/widgets/renderers/educationWidgetRenderers.tsx); the renderer passes no skin, because the card resolves its own from `data.skin` and that keeps the model inside the lazy education chunk | A different QUESTION about one shelf of named formulas: where is it (Reference Sheet's filtered two-column ledger), what does it say (Equation Cards' hero equation), how much fits (Exam Strip's packed columns), where did it come from (Derivation's numbered rail), is it consistent (Unit-aware's balance), what is the answer (Worked Example). [formulaSheetSkinModel.ts](../src/components/widgets/modules/formulaSheetSkinModel.ts) is the single answer for all six and for the folded tile. Its tokenizer is the root of everything: a capital opens a new symbol so `nRT` reads as n·R·T while `litres` stays whole, which then yields the symbol chips, the evaluable form (the shared evaluator has NO notion of implicit multiplication, so `nRT` must become `n*R*T`), and the unit walk. Units are parsed as products — `m/s` is metres over seconds, never one opaque word — then multiplied, inverted and raised through the expression; a symbol with no unit yet makes the branch `unchecked`, never `mismatch`, because a card must not call a formula wrong for being unfinished. The arithmetic is the Calculator's [calculatorSkinModel.ts](../src/components/widgets/modules/calculatorSkinModel.ts), which folds case, so a formula using both `V` and `v` is reported unsolvable rather than answered wrongly. `reference_sheet`/`equation_cards`/`exam_strip` are renderer-ready and add NOTHING to a record (the filter lives in component state); only `derivation`/`unit_aware`/`worked_example` persist, through `rendererOwnedSkinDetails`, and `dataWithoutFormula` takes all three skins' material with the formula it belonged to | [27-formula-sheet-skins.css](../src/styles/product/27-formula-sheet-skins.css): one continuous surface per skin with divider-separated rows rather than a stack of little panes, exactly one hero each (the equation, the result block, the answer), every control wrapper carries `gp-bare-field`, and the board's single typeface holds — an expression earns its texture from tabular figures and tracking, never a second family (`globalTypography.test.ts` enforces this). Per-skin heights live in the `formula_sheet` branch of [widgetSizing.ts](../src/store/widgetSizing.ts), measured from the rendered card and reading counts only, so sizing never has to load the model | [formulaSheetSkinModel.test.ts](../src/components/widgets/modules/formulaSheetSkinModel.test.ts) (including the two readings the skin gallery shows off), [formulaSheetSkins.test.ts](../src/widgets/formulaSheetSkins.test.ts), [catalogueFamilyFaces.test.ts](../src/utils/restingFaces/catalogueFamilyFaces.test.ts), then exercise all six skins, a formula deleted while wearing each, a symbol with no unit, a unit mismatch, an unsolvable example, narrow cards, and reduced motion |
| Unit Converter skins: General, Cooking, Engineering, Data, Temperature, Currency, Custom Formula | [UnitConverterWidget.tsx](../src/components/widgets/modules/UnitConverterWidget.tsx), dispatched by the `unit_converter` branch in [workflowWidgetRenderers.tsx](../src/components/widgets/renderers/workflowWidgetRenderers.tsx) and re-exported through [EssentialWidgets.tsx](../src/components/widgets/modules/EssentialWidgets.tsx) | `UnitConverterData.value` remains the one circuit-writable input and `unitConverterReading` in [unitConverterSkinModel.ts](../src/components/widgets/modules/unitConverterSkinModel.ts) is the single conversion truth for the renderer and `output` field. Old cards without `skin` remain General. Cooking owns kitchen volume plus shared weight/temperature; Engineering owns length/area/volume/pressure/energy/power; Data distinguishes decimal/binary storage and transfer rate; Temperature focuses the existing thermal transform. Currency is deliberately manual and dated so network failure cannot disturb local board work; its rate/code/date and Custom Formula's bounded factor/offset/unit names stay isolated in `skinStates`. Both schema-extension skins own their controls through `rendererOwnedSkinDetails`; appearance persists in `skin` | [28-unit-converter-skins.css](../src/styles/product/28-unit-converter-skins.css): input and output remain stable landmarks, the computed output is the one hero in a genuine well, every control wrapper carries `gp-bare-field`, category rails scroll before labels truncate, and narrow cards stack the conversion stage only at the smallest width | [unitConverterSkinModel.test.ts](../src/components/widgets/modules/unitConverterSkinModel.test.ts), [UnitConverterWidget.test.tsx](../src/components/widgets/modules/UnitConverterWidget.test.tsx), [unitConverterSkins.test.ts](../src/widgets/unitConverterSkins.test.ts), then exercise all seven skins, category and unit changes, reciprocal swaps, copy feedback, currency provenance, formula inversion, a wire write, narrow widths, both themes, and reduced motion |
| Dialog skins: Screenplay, Chat, Interview, Roleplay, Comic, Localization, Audio Transcript | [DialogWidget.tsx](../src/components/widgets/modules/DialogWidget.tsx), dispatched by the `dialog` branch in [coreWidgetRenderers.tsx](../src/components/widgets/renderers/coreWidgetRenderers.tsx) | Every skin edits the same canonical `DialogData.lines`, so switching appearances never changes a speaker or cue. Stable speaker ordering, bounded per-line detail lookup, fallback timecodes, and deleted-line cleanup live in [dialogSkinModel.ts](../src/components/widgets/modules/dialogSkinModel.ts). Roleplay directions, translations, target language, and transcript timestamps stay isolated in `skinStates`; their schema-extension views own their controls through `rendererOwnedSkinDetails`. `skinField` stays `'skin'`, and old unskinned cards fall back to Screenplay | [27-dialog-skins.css](../src/styles/product/27-dialog-skins.css): every view paints on one card backplate, control wrappers carry `gp-bare-field`, hover-only removal controls are inert until revealed, and container queries stack localization and comic layouts before text is squeezed | [dialogSkinModel.test.ts](../src/components/widgets/modules/dialogSkinModel.test.ts), [DialogWidget.test.tsx](../src/components/widgets/modules/DialogWidget.test.tsx), [dialogSkins.test.ts](../src/widgets/dialogSkins.test.ts), then exercise all seven skins, add/edit/remove, specialist fields, Cmd/Ctrl+Enter, narrow cards, both themes, keyboard focus, and reduced motion |
| Form skins: Intake, Survey, Feedback, RSVP, Inspection, Application, Conditional Form | [FormWidget.tsx](../src/components/widgets/modules/FormWidget.tsx) draws all seven presets, dispatched by the `form` branch in [workflowWidgetRenderers.tsx](../src/components/widgets/renderers/workflowWidgetRenderers.tsx) and re-exported through [EssentialWidgets.tsx](../src/components/widgets/modules/EssentialWidgets.tsx) | `FormWidgetData.title` and `fields` stay canonical, so a preset change never rewrites an answer. Bounded field normalization, completion/progress, feedback averages, inspection verdicts, application sections/evidence/review state, and conditional gating live in [formSkinModel.ts](../src/components/widgets/modules/formSkinModel.ts). Inspection pass/fail is read from the canonical checkbox answer so a circuit sees the inspector's verdict; only "n/a" and the note are skin state. Gates may only point at earlier questions, a gate whose source is deleted opens, and removing a field prunes its note, section, evidence, and any gate aimed at it. `filled_count`, `complete`, and the writable `first_value` stay in [professionalFields.ts](../src/widgets/fields/professionalFields.ts); the folded card reads each answer the way the worn preset does through [restingFace.ts](../src/utils/restingFace.ts), and per-preset creation heights live in [widgetSizing.ts](../src/store/widgetSizing.ts) | [27-form-skins.css](../src/styles/product/27-form-skins.css): one shell (eyebrow, title, completion rail) under every preset, `gp-bare-field` on every input wrapper, and container queries wrap field headers before labels truncate | [formSkins.test.ts](../src/widgets/formSkins.test.ts), then exercise all seven presets, add/edit/remove a field, a rating, an inspection verdict and n/a, an application section and review state, a conditional reveal, narrow cards, both themes, and reduced motion |
| Grades skins: Weighted Grade, GPA, Pass / Fail, What-if, Rubric, Dropped Scores, Curve Simulator | [GradeCalcWidget.tsx](../src/components/widgets/modules/GradeCalcWidget.tsx), with the shared GPA ledger in [GpaWidget.tsx](../src/components/widgets/modules/GpaWidget.tsx), dispatched by the `grade_calc` branch in [consolidatedWidgetRenderers.tsx](../src/components/widgets/renderers/consolidatedWidgetRenderers.tsx) | Coursework and GPA courses remain canonical and survive every skin switch. Pure grade, GPA, scenario, curve, and drop-policy math plus persisted-state bounds live in [gradeSkinModel.ts](../src/components/widgets/modules/gradeSkinModel.ts). Pass mark, hypothetical score, drop count, and curve points are preview settings isolated under their own `skinStates`; none rewrites or removes a saved mark. Rubric edits the same canonical coursework through a purpose-built arrangement, and every schema-extension skin owns its controls through `rendererOwnedSkinDetails` | [27-grade-skins.css](../src/styles/product/27-grade-skins.css): computed answers use one quiet well, rows and scenario controls remain on the card backplate, every input wrapper carries `gp-bare-field`, and container queries keep labels and numeric controls usable on narrow cards | [gradeSkinModel.test.ts](../src/components/widgets/modules/gradeSkinModel.test.ts), [GradeCalcWidget.test.tsx](../src/components/widgets/modules/GradeCalcWidget.test.tsx), [gradeSkins.test.ts](../src/widgets/gradeSkins.test.ts), then exercise all seven skins, add/edit/remove, threshold/what-if/drop/curve controls, narrow cards, both themes, keyboard focus, and reduced motion |
| Date skins: Date & Time, Deadline, Anniversary, Relative Date, Range, Recurring Date, Milestone; countdown-to, next occurrence, repeat rule, date span | [DateWidget.tsx](../src/components/widgets/modules/DateWidget.tsx), dispatched by the `date_picker` branch in [consolidatedWidgetRenderers.tsx](../src/components/widgets/renderers/consolidatedWidgetRenderers.tsx) and re-exported through [EssentialWidgets.tsx](../src/components/widgets/modules/EssentialWidgets.tsx) so it stays in the same lazy chunk | `DatePickerData.date` is the one canonical day: no skin stores a second copy of it. Anniversary and Recurring Date DERIVE their next occurrence, Range keeps only the far end, Deadline keeps only the runway it measures against, and Milestone keeps owner/status/deliverable — all in isolated `skinStates`, all owned by [dateSkinModel.ts](../src/components/widgets/modules/dateSkinModel.ts). `dateReading` is the single answer: the renderer's hero, the folded tile in [restingFace.ts](../src/utils/restingFace.ts), and `days_until`/`is_due`/`next_occurrence`/`duration_days` in [inputLogicFields.ts](../src/widgets/fields/inputLogicFields.ts) all read it, so a yearly occasion never reports itself overdue and the card, the tile and the wire cannot disagree. Distances are local-calendar days, rounded, so a DST boundary inside a span cannot lose an hour ([localDate.ts](../src/utils/localDate.ts)); a leap-day anniversary clamps to 28 Feb and a monthly rule counts from its original day-of-month rather than creeping. `skinField` stays `mode`, the field old boards already persist, and the retired `'countdown'` value reads back as Deadline and settles on first edit. Range, Recurring Date, and Milestone are `rendererOwnedSkinDetails` | [29-date-skins.css](../src/styles/product/29-date-skins.css): the reading is the one hero, every control is a sunken region of the single backplate with `gp-bare-field` on the wrapper that holds it, `--gp-date-tone` is the one hue that state re-points (late, due, shipped), and container queries stack paired day fields and drop the milestone rail's words before a date can truncate | [dateSkinModel.test.ts](../src/components/widgets/modules/dateSkinModel.test.ts), [dateSkins.test.ts](../src/widgets/dateSkins.test.ts), then exercise all seven skins, the quick-set chips, a runway change, a repeat-rule change, both range ends, a wire write while a skin is worn, an unset card, narrow cards, both themes, and reduced motion |
| Logbook skins: Daily Log, Incident Log, Lab Notebook, Change Log, Maintenance Log, Audit Trail, Travel Log | [LogbookWidget.tsx](../src/components/widgets/modules/LogbookWidget.tsx), dispatched by the `logbook` branch in [workflowWidgetRenderers.tsx](../src/components/widgets/renderers/workflowWidgetRenderers.tsx) and re-exported through [EssentialWidgets.tsx](../src/components/widgets/modules/EssentialWidgets.tsx) | `LogbookData.entries` remains the one canonical chronological record. Ordering, bounded untrusted-entry normalization, day grouping, append/removal, warning counts, and per-entry specialist details live in [logbookSkinModel.ts](../src/components/widgets/modules/logbookSkinModel.ts). Incident impact/response/resolution, lab hypothesis/method/observation/conclusion, change version/author, maintenance asset/parts/service date, audit actor/source, and travel place/distance/context stay isolated by skin and stable entry id in `skinStates`; removing an entry prunes every orphaned detail pocket. Audit Trail is append-only in its own view. The change type cycles through a colour-coded chip rather than a native `select` (a dropdown is a second material inside the card's glass), and Maintenance derives an overdue/due-soon badge from `nextService` — a readout in `logbookServiceDue`, not stored state, read by both the open card and the resting tile. Entry count, latest, warnings, latest level, and a circuit-writable append field live in [professionalFields.ts](../src/widgets/fields/professionalFields.ts), and per-skin resting faces live in [logbook.ts](../src/utils/restingFaces/logbook.ts): every skin folds into the shared rows grammar with its own eyebrow, lead, trailing value, and tone (incident status, lab stage, change version/kind, service due, audit actor, travel place), honouring the skin's own entry order | [26-logbook-skins.css](../src/styles/product/26-logbook-skins.css): rows are divider-separated records on one card backplate, `WidgetPanel` remains only as the stable removal-transition owner, every control wrapper carries `gp-bare-field`, and container queries collapse specialist grids and split headers into two lines before text truncates. Type follows Article XVII (9.5px labels, 13px body) — the catalogue's `!important` island paint, ghost-button fill, and 28px control floor are reclaimed deliberately: chips route their fill through `--gp-log-chip`, and dots keep an 18px face inside a 28px target via `background-clip: content-box`. The hover remove control is `pointer-events: none` until its row is hovered or focused, and the hover wash is scoped away from `.gp-subpanel-exit` so the removal transition still finalizes | [logbookSkinModel.test.ts](../src/components/widgets/modules/logbookSkinModel.test.ts), [LogbookWidget.test.tsx](../src/components/widgets/modules/LogbookWidget.test.tsx), [logbookSkins.test.ts](../src/widgets/logbookSkins.test.ts), then exercise all seven skins, append/edit/remove, level/status/order cycling, a circuit append, audit immutability, narrow cards, both themes, and reduced motion |
| Meeting Notes skins: Agenda, Minutes, Stand-up, Retrospective, One-to-One, Decision Review, Handoff | [MeetingNotesWidget.tsx](../src/components/widgets/modules/MeetingNotesWidget.tsx) draws all seven bodies, dispatched by the `meeting_notes` branch in [coreWidgetRenderers.tsx](../src/components/widgets/renderers/coreWidgetRenderers.tsx) | `date`, `attendees`, `notes`, and `actions` stay canonical and every skin shows all four — the notes field and the action list are simply relabelled per shape, so rolling a skin can never hide what someone wrote. Validation, attendee splitting, per-skin extras, and derived readings live in [meetingNotesSkinModel.ts](../src/components/widgets/modules/meetingNotesSkinModel.ts). Agenda timebox/outcome, minutes and one-to-one owner/due, decision rationale/review date, stand-up yesterday/blockers, retro improve/learned, one-to-one feedback/follow-up, and handoff risks/acknowledgement stay isolated per skin in `skinStates` (`items` keyed by action id, panels alongside); removing an action prunes its pocket from every skin. [planningWidgets.ts](../src/widgets/registry/planningWidgets.ts) attaches them with `skinField: 'skin'` because appearance must never move what circuits read, and `actions_done` in [dataMediaFields.ts](../src/widgets/fields/dataMediaFields.ts) reads the same actions under every shape. The folded card keeps the arrangement its skin reads the meeting in through [meeting.ts](../src/utils/restingFaces/meeting.ts): an agenda folds to numbered topic rows with timeboxes, minutes to ruled ledger lines, a stand-up to its three lanes plus asks, a retro to 2×2 quadrant columns, and the review/handoff shapes carry their due-warning and sign-off eyebrows — every reading through meetingNotesSkinModel, every shape an existing grammar in [restingFaceModel.ts](../src/utils/restingFaceModel.ts), so no new face kinds or CSS | [27-meeting-notes-skins.css](../src/styles/product/27-meeting-notes-skins.css): an agenda topic is a timeline stop, a resolution a ruled ledger line, a decision a card with a review clock; every control wrapper carries `gp-bare-field`, `WidgetPanel` stays only as the removal-transition owner, and container queries collapse the retro quadrants and one-to-one columns before text truncates | [meetingNotesSkinModel.test.ts](../src/components/widgets/modules/meetingNotesSkinModel.test.ts), [MeetingNotesWidget.test.tsx](../src/components/widgets/modules/MeetingNotesWidget.test.tsx), [meetingNotesSkins.test.ts](../src/widgets/meetingNotesSkins.test.ts), then roll one card through all seven skins and back and confirm the date, attendees, notes, and every action survive, plus add/complete/remove, timebox totals, review-date warning, handoff acknowledgement, narrow cards, both themes, and reduced motion |
| Pros & Cons skins: Balance, Debate, Red Team, Weighted Trade-off, Reversible / Irreversible | [ProsConsWidget.tsx](../src/components/widgets/modules/ProsConsWidget.tsx) draws all five, dispatched by the `pros_cons` branch in [coreWidgetRenderers.tsx](../src/components/widgets/renderers/coreWidgetRenderers.tsx) | `ProsConsData.topic`, `pros`, and `cons` stay canonical, so every skin edits the same arguments. Bounded item normalization, plain and weighted verdicts, debate pairing/rebuttals, red-team severity and unanswered-evidence exposure, 1-5 importance weights, and reversibility live in [prosConsSkinModel.ts](../src/components/widgets/modules/prosConsSkinModel.ts). Rebuttals, severities, weights, and one-way verdicts stay isolated by skin and item id in `skinStates`; removing a point prunes every detail hanging off it. Empty draft rows never tip the balance, and an unmarked consequence reads as reversible. `pros_count`/`cons_count` in [dataMediaFields.ts](../src/widgets/fields/dataMediaFields.ts) are unchanged by the worn skin; the folded card shows the number the skin is about through [restingFace.ts](../src/utils/restingFace.ts), and per-skin creation heights live in [widgetSizing.ts](../src/store/widgetSizing.ts) | [27-pros-cons-skins.css](../src/styles/product/27-pros-cons-skins.css): pro reads green and con rose in every skin, one shared balance meter serves both scored skins, and container queries stack the paired columns before text truncates | [prosConsSkins.test.ts](../src/widgets/prosConsSkins.test.ts), then exercise all five skins, add/edit/remove a point, a rebuttal, severity and evidence, the weight dial, a reversibility flip, narrow cards, both themes, and reduced motion |
| Outline skins: Tree, Roman, Scenes, Sitemap, Course, Work Breakdown, Collapsible Brief | [OutlineWidget.tsx](../src/components/widgets/modules/OutlineWidget.tsx), dispatched by the `outline` branch in [workflowWidgetRenderers.tsx](../src/components/widgets/renderers/workflowWidgetRenderers.tsx) and re-exported through [EssentialWidgets.tsx](../src/components/widgets/modules/EssentialWidgets.tsx) | `OutlineData.items` remains the one canonical ordered hierarchy, so every skin shares text, depth, branch collapse, keyboard insertion, indentation, and deletion. Bounded untrusted-data normalization, hidden-branch resolution, sibling numbering, skin-specific hierarchy language, and specialist-state cleanup live in [outlineSkinModel.ts](../src/components/widgets/modules/outlineSkinModel.ts). Work Breakdown owner/effort/completion and Collapsible Brief notes/attachments/disclosure stay isolated by skin and stable item id in `skinStates`; both own their specialist controls through `rendererOwnedSkinDetails`. The folded card keeps the worn skin's decoration of the same visible rows — roman markers as leads, act accents, delivery checkmarks/estimates and a completion meter — through [outline.ts](../src/utils/restingFaces/outline.ts) | [27-outline-skins.css](../src/styles/product/27-outline-skins.css): one continuous divider-separated surface, skin-specific markers and typography, hover chrome that is pointer-inert while hidden, compact container-query fallbacks, and reduced-motion-safe transitions | [outlineSkinModel.test.ts](../src/components/widgets/modules/outlineSkinModel.test.ts), [OutlineWidget.test.tsx](../src/components/widgets/modules/OutlineWidget.test.tsx), [outlineSkins.test.ts](../src/widgets/outlineSkins.test.ts), then exercise all seven skins, Enter/Backspace/Tab/Shift-Tab, branch and brief disclosure, work metadata/completion, narrow cards, both themes, and reduced motion |
| Poll skins: Bars, Donut, Approval, Ranked Choice, Pairwise, Live Room, Anonymous | [PollWidget.tsx](../src/components/widgets/modules/PollWidget.tsx), dispatched by the `poll` branch in [coreWidgetRenderers.tsx](../src/components/widgets/renderers/coreWidgetRenderers.tsx) | `PollData.options[].votes` is the one canonical tally and every skin writes it, so a ranked ballot leaves first-preference counts and a duel leaves the winner's count behind — switching skins never loses a vote. Bounded option/skin validation, standings and shares, approval ballots, instant-runoff rounds, pairwise duel records and queue, live-room phase, and anonymous aggregation live in [pollSkinModel.ts](../src/components/widgets/modules/pollSkinModel.ts). Ranked ballots, duel records, room participants/phase/reveal, and ballot counts stay isolated per skin in `skinStates`; removing an option prunes it from every ballot and duel, and the `reset` command in [coreCommands.ts](../src/widgets/fields/coreCommands.ts) clears those ledgers with the votes. `skinField` is `'skin'` and every schema-extension skin owns its controls through `rendererOwnedSkinDetails`. Total, leader, and leading share are published in [coreWidgetFields.ts](../src/widgets/fields/coreWidgetFields.ts); the folded card rests as its real options with their share through [restingFace.ts](../src/utils/restingFace.ts) and masks an unrevealed Live Room result | [27-poll-skins.css](../src/styles/product/27-poll-skins.css): one stable colour per option across all seven skins, bars and dial share the `gp-poll-track` fill, unrevealed results are blurred rather than removed. The shared control sheets set the geometry: text and icon actions are ghost controls on a 28px hit floor, so rows reserve 28px lanes and removal is a hover-revealed overlay; only the swatch, ballot marks, and duel cards opt out with `gp-check-free`; inputs undo the field-well padding under `gp-bare-field`. Container queries stack the donut legend and the ranked ballot/runoff column, and Ranked Choice alone lays out as a flex column so its two lists grow the card instead of squeezing each other | [pollSkinModel.test.ts](../src/components/widgets/modules/pollSkinModel.test.ts), [PollWidget.test.tsx](../src/components/widgets/modules/PollWidget.test.tsx), [pollSkins.test.ts](../src/widgets/pollSkins.test.ts), then exercise all seven skins, vote/add/remove/relabel, an approval ballot, a runoff to majority, a duel and its matrix, room open/close/reveal, an anonymous cast and hand-off, narrow cards, both themes, keyboard focus, and reduced motion |
| Text Input skins: Single Line, Multiline, Search, URL, Email, Tags, Command | [TextInputWidget.tsx](../src/components/widgets/modules/TextInputWidget.tsx), dispatched by the `text_input` branch in [workflowWidgetRenderers.tsx](../src/components/widgets/renderers/workflowWidgetRenderers.tsx) and re-exported through [EssentialWidgets.tsx](../src/components/widgets/modules/EssentialWidgets.tsx) so it stays in the same lazy chunk | `TextInputData.value` is the circuit's one canonical string — every skin reads and writes exactly it, and every skin keeps it on screen. Address parsing (only `http(s)` ever reaches an `href`), email shape, tag normalization, and the bounded command history live in [textInputSkinModel.ts](../src/components/widgets/modules/textInputSkinModel.ts), which also reads an unskinned old card from its saved `multiline` boolean and gives [restingFace.ts](../src/utils/restingFace.ts) the emitted string so a folded card stops resting as a blank icon. Tags are a reading of the same comma-separated value, not a second copy; only Command keeps state of its own (draft and history) in isolated `skinStates`. Tags and Command are `rendererOwnedSkinDetails`, and `skinField` stays `'skin'` because `TextInputData` has no `mode` | [22-text-input-skins.css](../src/styles/product/22-text-input-skins.css): the search pill and command prompt are shaped surfaces painted through a pseudo-element, because every wrapper holding a text control carries `gp-bare-field` and that escape hatch clears backgrounds with `!important` | [textInputSkinModel.test.ts](../src/components/widgets/modules/textInputSkinModel.test.ts), [TextInputWidget.test.tsx](../src/components/widgets/modules/TextInputWidget.test.tsx), [textInputSkins.test.ts](../src/widgets/textInputSkins.test.ts), then exercise every skin, an unsafe address, Enter/Backspace in the tag row, a command run and re-run, narrow and short cards, both themes, and reduced motion |
| Media skins: Image, Video, Audio, Document Preview, Before / After, Gallery, Moodboard | [MediaWidget.tsx](../src/components/widgets/modules/MediaWidget.tsx), dispatched by the `media` branch in [coreWidgetRenderers.tsx](../src/components/widgets/renderers/coreWidgetRenderers.tsx) | `MediaData.url`, `caption`, and `altText` stay canonical; the comparison's second image, the gallery's items, the moodboard's placements, and the video poster live in isolated `skinStates`. Address validation, file/host/kind derivation, playback formatting, and item bounds live in [mediaSkinModel.ts](../src/components/widgets/modules/mediaSkinModel.ts) — persisted addresses are untrusted, so only `http(s)`, `blob:`, and `data:image|video|audio` ever reach a `src`, and only `http(s)` ever reaches an `href`. Gallery and Moodboard are `rendererOwnedSkinDetails`, and `skinField` stays `'skin'` because `MediaData` has no `mode` | [14-media-skins.css](../src/styles/product/14-media-skins.css): the stage bleeds through the backplate inset and clips to the card radius, and every wrapper holding a text control carries `gp-bare-field` | [mediaSkinModel.test.ts](../src/components/widgets/modules/mediaSkinModel.test.ts), [MediaWidget.test.tsx](../src/components/widgets/modules/MediaWidget.test.tsx), [mediaSkins.test.ts](../src/widgets/mediaSkins.test.ts), then exercise every skin, an empty card, narrow widths, keyboard reveal/scrub, moodboard drag, and reduced motion |
| Canvas-card skins: Portal, Cover, Live Thumbnail, Dashboard Door, Folder Index | [CanvasNodeWidget.tsx](../src/components/widgets/modules/CanvasNodeWidget.tsx), dispatched by the `canvas_node` branch in [coreWidgetRenderers.tsx](../src/components/widgets/renderers/coreWidgetRenderers.tsx) | The backing `canvasId` remains shared. Cover copy is isolated under `skinStates.cover`; child-card counts, completion, attention signals, type frequency, nested canvases, and normalized thumbnail geometry derive read-only from canonical canvases/widgets through [canvasNodeSkinModel.ts](../src/components/widgets/modules/canvasNodeSkinModel.ts). The three schema-extension skins own their UI through `rendererOwnedSkinDetails`, so no generic editor covers the preview | [10-canvas-node-skins.css](../src/styles/product/10-canvas-node-skins.css), following the one-backplate, one-hero, and single-ledger-surface laws | [canvasNodeSkinModel.test.ts](../src/components/widgets/modules/canvasNodeSkinModel.test.ts), [CanvasNodeWidget.test.tsx](../src/components/widgets/modules/CanvasNodeWidget.test.tsx), [canvasNodeSkins.test.ts](../src/widgets/canvasNodeSkins.test.ts), then exercise all five skins, child navigation, empty/populated states, narrow widths, both themes, and reduced motion |
| Bullets skins: Dots, Numbered, Nested Outline | [BulletsWidget.tsx](../src/components/widgets/modules/BulletsWidget.tsx), dispatched by the `bullets` branch in [coreWidgetRenderers.tsx](../src/components/widgets/renderers/coreWidgetRenderers.tsx) | Every skin edits the same `BulletsData.items`. A point is a PARAGRAPH: each row is a self-fitting `textarea`, re-measured before the card reports its own height, so nothing is ever clipped; the card is width-draggable to a six-cell floor with `autoHeight` still owning the height. Outline levels/collapse are sanitized and isolated under their own `skinStates` by [bulletSkinModel.ts](../src/components/widgets/modules/bulletSkinModel.ts), which also folds the three retired skins (Compact Chips, Two Column, Rolling Log) back to Dots for boards that still name them; circuit-added bullets preserve the worn skin and its state. Outline owns its controls through `rendererOwnedSkinDetails`, so the generic schema editor never doubles them | [11-bullet-skins.css](../src/styles/product/11-bullet-skins.css); rows share one divider-separated list surface, align to their first line, and the add control is a bare `+`, while `WidgetPanel` remains only as the stable removal-transition owner | [bulletSkinModel.test.ts](../src/components/widgets/modules/bulletSkinModel.test.ts), [BulletsWidget.test.tsx](../src/components/widgets/modules/BulletsWidget.test.tsx), [bulletSkins.test.ts](../src/widgets/bulletSkins.test.ts), then exercise all three skins, Enter/Backspace, indent/outdent/collapse, a point long enough to wrap, the six-cell width floor, both themes, and reduced motion |
| Tasks skins: List, Inbox, Shopping, Assignments, Day, Week, Board, Timeline, Priority Matrix, Recurring, Sprint, Dependencies, Routine | [TasksWidget.tsx](../src/components/widgets/modules/TasksWidget.tsx) dispatches every skin from the `checklist` branch in [consolidatedWidgetRenderers.tsx](../src/components/widgets/renderers/consolidatedWidgetRenderers.tsx); the five two-dimensional arrangements live in [TasksSpatialSkins.tsx](../src/components/widgets/modules/TasksSpatialSkins.tsx) | Every skin edits the same `ChecklistData.items`, and `item.done` is the one completion truth — `taskStatus` in [taskSkinModel.ts](../src/components/widgets/modules/taskSkinModel.ts) lets `done` overrule a stale `status` so a Board and the Done-count port can never disagree. That model also owns due-date reading (local calendar days, never UTC), assignment/day ordering, week/quadrant/span clamping, and the sanitizing of each schema-extension skin's isolated `skinStates` slot: shopping quantities, repeat rules, sprint owners/estimates, blocker links (self-links, dead links, and cycle-closing links are dropped), and routine run counts. Recurring is the one skin where ticking a task reschedules it instead of closing it. `rendererOwnedSkinDetails` covers Recurring, Sprint, Dependencies, and Routine so the generic schema-extension overlay never doubles their controls; `skinField` stays `mode`, the field `ChecklistData` has always persisted. `SPATIAL_SKINS` and the registry's `sizing.fixed` predicate in [planningWidgets.ts](../src/widgets/registry/planningWidgets.ts) are held in agreement by a test. The former standalone `kanban`/`assignment`/`daily_agenda`/`weekly_planner`/`timeline`/`priority_matrix` cards stay registered for old-board hydration but no longer render Tasks | [21-task-skins.css](../src/styles/product/21-task-skins.css): rows, cards, and columns are shaded regions of one list surface, `WidgetPanel` stays only as the removal-transition owner, and colour is spent only on state that means something — done, blocked, overdue, the step you are on. Container queries fold Week into labelled day rows and Board/Sprint into stacked columns before a task name can be truncated | [taskSkinModel.test.ts](../src/components/widgets/modules/taskSkinModel.test.ts), [TasksWidget.test.tsx](../src/components/widgets/modules/TasksWidget.test.tsx), [taskSkins.test.ts](../src/widgets/taskSkins.test.ts), then exercise all thirteen skins, Enter/Backspace, status cycling, quantity/estimate/blocker/repeat editing, a routine run, a wire write while a skin is worn, narrow cards, both themes, and reduced motion |
| Habit Tracker skins: Week Grid, Month Heatmap, Chain, Scorecard, Routine Stack, Minimum / Target, Flexible Frequency | [HabitWidget.tsx](../src/components/widgets/modules/HabitWidget.tsx), dispatched by the `habit` branch in [coreWidgetRenderers.tsx](../src/components/widgets/renderers/coreWidgetRenderers.tsx) | `HabitData.days` remains the canonical Monday–Sunday completion week and `streak` remains its circuit-facing completed count. Untrusted week normalization, completion/chain readings, bounded routine steps, daily amounts, and flexible target validation live in [habitSkinModel.ts](../src/components/widgets/modules/habitSkinModel.ts). Routine step progress and Minimum / Target amounts derive each canonical day instead of competing with it, and all specialist state is isolated by skin. The three schema-extension skins own their controls through `rendererOwnedSkinDetails`; `skinField` stays `skin` so appearance never collides with habit state | [27-habit-skins.css](../src/styles/product/27-habit-skins.css): every skin paints on one calm backplate, checked days are the only strong accent, responsive container rules preserve the seven equal day choices, and every text-control wrapper carries `gp-bare-field` | [habitSkinModel.test.ts](../src/components/widgets/modules/habitSkinModel.test.ts), [HabitWidget.test.tsx](../src/components/widgets/modules/HabitWidget.test.tsx), [habitSkins.test.ts](../src/widgets/habitSkins.test.ts), then exercise all seven skins, skin switching without completion loss, routine steps, minimum/target counts, flexible frequency, narrow cards, both themes, keyboard focus, and reduced motion |
| Calendar skins: Month, Week, Agenda, Year Heatmap, Availability, Shift Rota, Birthday & Anniversary, Connected Calendars | [CalendarWidget.tsx](../src/components/widgets/modules/CalendarWidget.tsx), dispatched by the `calendar` branch in [coreWidgetRenderers.tsx](../src/components/widgets/renderers/coreWidgetRenderers.tsx) | Every board-owned view shares `CalendarData.markedDates`; availability blocks, week anchors, rota assignments, and recurring occasions are sanitized and isolated per skin by [calendarSkinModel.ts](../src/components/widgets/modules/calendarSkinModel.ts). Connected Calendars is deliberately viewer-local: [externalCalendarService.ts](../src/services/externalCalendarService.ts) requests a read-only Google Calendar scope, keeps provider tokens in device storage, fetches a bounded visible-month agenda, and never writes external event details into the shared board. Rota and occasions own their specialist controls through `rendererOwnedSkinDetails`, so the generic schema editor never overlaps them. All date arithmetic stays local-calendar based and never parses day keys through UTC | [12-calendar-skins.css](../src/styles/product/12-calendar-skins.css); each view uses one continuous working surface with responsive compact labels | [calendarSkinModel.test.ts](../src/components/widgets/modules/calendarSkinModel.test.ts), [externalCalendarService.test.ts](../src/services/externalCalendarService.test.ts), [CalendarWidget.test.tsx](../src/components/widgets/modules/CalendarWidget.test.tsx), [calendarSkins.test.ts](../src/widgets/calendarSkins.test.ts), then exercise all eight skins, Google connect-refresh-disconnect, marking/painting, period navigation, specialist editors, narrow widths, both themes, and reduced motion |
| College Canvas LMS skins: Overview, Courses, Assignments, Grades, Announcements | [CanvasLmsWidget.tsx](../src/components/widgets/modules/CanvasLmsWidget.tsx), dispatched by the `canvas_lms` branch in [educationWidgetRenderers.tsx](../src/components/widgets/renderers/educationWidgetRenderers.tsx) | The board persists only `CanvasLmsData.skin`. [canvasLmsService.ts](../src/services/canvasLmsService.ts) owns the HTTPS school origin, device-local bearer token, bounded Canvas REST reads, response validation, and disconnect cleanup. Website reads pass through the same-origin, no-storage Cloudflare relay in [canvasRelay.ts](../worker/canvasRelay.ts), which permits only the widget's three read-only API paths, refuses redirects, bounds payloads, and rate-limits a one-way token hash. [index.ts](../worker/index.ts) is deployed separately through [wrangler.canvas-relay.toml](../wrangler.canvas-relay.toml) on the more-specific API route, so a relay deployment never republishes the website's static assets; [vite.config.ts](../vite.config.ts) proxies that same endpoint during local web development. The token is never persisted by the relay. Courses, planner items, grades, and announcements stay in component memory and are never written to the board, collaboration sync, or circuit fields. [canvasLmsModel.ts](../src/components/widgets/modules/canvasLmsModel.ts) owns the five-value skin fallback and assignment ordering. The folded face in [canvasLms.ts](../src/utils/restingFaces/canvasLms.ts) deliberately shows only the privacy contract, never student data | [34-canvas-lms-skins.css](../src/styles/product/34-canvas-lms-skins.css): a single continuous course surface, restrained course-color markers, and narrow-card stacking | [canvasLmsService.test.ts](../src/services/canvasLmsService.test.ts), [canvasRelay.test.ts](../worker/canvasRelay.test.ts), [canvasLmsModel.test.ts](../src/components/widgets/modules/canvasLmsModel.test.ts), [CanvasLmsWidget.test.tsx](../src/components/widgets/modules/CanvasLmsWidget.test.tsx), [canvasLmsSkins.test.ts](../src/widgets/canvasLmsSkins.test.ts), then connect/refresh/disconnect against a real school Canvas account, exercise all five skins, narrow widths, both themes, keyboard focus, and reduced motion |
| Chart skins: Bar, Line, Donut, Pie, Area, Sparkline, Gauge, Progress Ring, Heatmap, Scatter, Stacked | [BarChartWidget.tsx](../src/components/widgets/modules/BarChartWidget.tsx), dispatched by the `bar_chart` branch in [consolidatedWidgetRenderers.tsx](../src/components/widgets/renderers/consolidatedWidgetRenderers.tsx) | Every visualization reads and edits the same `BarChartData.bars`; safe domains, SVG paths, shares, trend math, gauge bounds, heatmap columns, scatter coordinates, and stacked series validation live in [chartSkinModel.ts](../src/components/widgets/modules/chartSkinModel.ts). Advanced settings remain isolated in `skinStates`, and Heatmap, Scatter, and Stacked own their specialist editor through `rendererOwnedSkinDetails` | [13-chart-skins.css](../src/styles/product/13-chart-skins.css); the chart remains the only main surface and a shared data studio overlays it only on request | [chartSkinModel.test.ts](../src/components/widgets/modules/chartSkinModel.test.ts), [BarChartWidget.test.tsx](../src/components/widgets/modules/BarChartWidget.test.tsx), [chartSkins.test.ts](../src/widgets/chartSkins.test.ts), then exercise all eleven skins, the shared editor, negative/zero values, specialist state, compact widths, both themes, and reduced motion |
| Table skins: Grid, Compact Ledger, Cards, Database, Kanban, Gallery, Form View, Pivot | [TableWidget.tsx](../src/components/widgets/modules/TableWidget.tsx), dispatched by the `table` branch in [coreWidgetRenderers.tsx](../src/components/widgets/renderers/coreWidgetRenderers.tsx) | Every view edits the same `TableData.rows`; ragged-row normalization, column type inference, database search/sort, grouped Kanban records, gallery/form selection, and pivot aggregation live in [tableSkinModel.ts](../src/components/widgets/modules/tableSkinModel.ts). Advanced view settings remain isolated in `skinStates`, and all five schema-extension views own their specialist controls through `rendererOwnedSkinDetails`; editing cells always spreads the existing data so the worn skin and its settings survive | [15-table-skins.css](../src/styles/product/15-table-skins.css); one shared table studio overlays any view on request, while Grid and Ledger stay directly keyboard-editable | [tableSkinModel.test.ts](../src/components/widgets/modules/tableSkinModel.test.ts), [TableWidget.test.tsx](../src/components/widgets/modules/TableWidget.test.tsx), [tableSkins.test.ts](../src/widgets/tableSkins.test.ts), then exercise all eight skins, keyboard movement, search/sort/group/config controls, studio edits, compact widths, both themes, and reduced motion |
| Metrics skins: KPI Tiles, Big Number, Scoreboard, Traffic Lights, Delta, Target, Executive Strip | [MetricsWidget.tsx](../src/components/widgets/modules/MetricsWidget.tsx), dispatched by the `metrics` branch in [coreWidgetRenderers.tsx](../src/components/widgets/renderers/coreWidgetRenderers.tsx) | Every view edits the same circuit-writable `MetricsData.tiles`; numeric parsing, summaries, comparisons, goals, ownership, and freshness validation live in [metricsSkinModel.ts](../src/components/widgets/modules/metricsSkinModel.ts). Delta baselines, Targets, and Executive ownership/freshness remain isolated in `skinStates` and own their specialist controls through `rendererOwnedSkinDetails`; every canonical edit and circuit write spreads existing data so appearance and specialist state survive | [16-metrics-skins.css](../src/styles/product/16-metrics-skins.css); a shared Metrics Studio overlays every view on request, while trend signals stay directly operable in the presentation | [metricsSkinModel.test.ts](../src/components/widgets/modules/metricsSkinModel.test.ts), [MetricsWidget.test.tsx](../src/components/widgets/modules/MetricsWidget.test.tsx), [metricsSkins.test.ts](../src/widgets/metricsSkins.test.ts), then exercise all seven skins, trend cycling, specialist settings, studio edits, empty/narrow states, both themes, and reduced motion |
| Budget skins: Category Plan, Envelope, Zero-based, 50 / 30 / 20, Cashflow, Sinking Funds, Shared Budget, Project Budget | [BudgetWidget.tsx](../src/components/widgets/modules/BudgetWidget.tsx), dispatched by the `budget` branch in [coreWidgetRenderers.tsx](../src/components/widgets/renderers/coreWidgetRenderers.tsx) | Every view shares `BudgetData.items`, so the existing Total circuit output remains the one sum. Actual/spent amounts, income, rule buckets, dates and cashflow kinds, saved balances and due dates, contributors, and project stages are sanitized and isolated per skin by [budgetSkinModel.ts](../src/components/widgets/modules/budgetSkinModel.ts). A shared Budget Studio edits canonical labels and amounts; schema-extension views own their specialist controls through `rendererOwnedSkinDetails`. The folded card shows the number the worn lens is about — spend bars under the plan-remaining verdict, dollars left to assign, money in against money out — through [budget.ts](../src/utils/restingFaces/budget.ts), which derives every reading from the same `budgetSkinModel` functions the open card renders | [27-budget-skins.css](../src/styles/product/27-budget-skins.css); each skin paints one continuous financial surface with responsive compact arrangements, meaningful state colour, and reduced-motion support | [budgetSkinModel.test.ts](../src/components/widgets/modules/budgetSkinModel.test.ts), [BudgetWidget.test.tsx](../src/components/widgets/modules/BudgetWidget.test.tsx), [budgetSkins.test.ts](../src/widgets/budgetSkins.test.ts), then exercise all eight skins, studio add/edit/remove, specialist controls, skin switching, narrow cards, both themes, and reduced motion |
| Location skins: Pin, Coordinates, Local Time, Compass, Geofence, Route | [LocationWidget.tsx](../src/components/widgets/modules/LocationWidget.tsx), dispatched by the `location` entry in [catalogWidgetRenderers.tsx](../src/components/widgets/renderers/catalogWidgetRenderers.tsx) (it left the generic `ExpansionWidget` grid when it gained skins) | Every skin reads and writes the same canonical `LocationData` label, address, latitude, longitude, and timezone, so a wire cannot tell which skin is worn; registry/defaults live in [expansion.ts](../src/widgets/registry/expansion.ts) and the data contract in [widgetDataExpansion.ts](../src/types/widgetDataExpansion.ts). Distance, bearing, notation, timezone readings, NOAA sunrise/sunset, fence verdicts, and route legs are pure in [locationSkinModel.ts](../src/components/widgets/modules/locationSkinModel.ts); the coordinate notation, fence radius, and route stops are isolated in `skinStates` and own their controls through `rendererOwnedSkinDetails`. Bindable address, latitude, longitude, timezone, coordinate string, map URL, availability, accuracy, and captured time stay in [fields/expansion.ts](../src/widgets/fields/expansion.ts). Browser geolocation is local UI state only — a refused permission, a missing sensor, or a timeout must never disturb the stored place, and nothing writes the pin without a press | [23-location-skins.css](../src/styles/product/23-location-skins.css); the dial, fence ring, and day arc are flat visuals that own their paint, and every wrapper holding a text control carries `gp-bare-field` | [locationSkinModel.test.ts](../src/components/widgets/modules/locationSkinModel.test.ts), [LocationWidget.test.tsx](../src/components/widgets/modules/LocationWidget.test.tsx), [locationSkins.test.ts](../src/widgets/locationSkins.test.ts), [locationWidget.test.ts](../src/widgets/locationWidget.test.ts), then exercise all six skins, permission allowed/denied, manual coordinates, Follow me, fence radius, stop reorder, narrow widths, and reduced motion |
| Trip Itinerary skins: Days, Timeline, Bookings, Offline, Map, Group, Travel Day | [TripItineraryWidget.tsx](../src/components/widgets/modules/TripItineraryWidget.tsx), dispatched by the `trip_itinerary` entry in [catalogWidgetRenderers.tsx](../src/components/widgets/renderers/catalogWidgetRenderers.tsx) (it left the generic `ExpansionWidget` grid when it gained skins) | `TripItineraryData.days` (dated days holding time/what/where/confirmation/booked legs) remains the one canonical itinerary every skin and wire reads; registry, hand-declared skins, and defaults live in [expansion.ts](../src/widgets/registry/expansion.ts) and the data contract in [widgetDataExpansion.ts](../src/types/widgetDataExpansion.ts). Normalization, chronological flattening, trip-phase and day-number readouts, add/remove for days and legs, buffered leave-by arithmetic, zone clustering, and people collection are pure in [tripItinerarySkinModel.ts](../src/components/widgets/modules/tripItinerarySkinModel.ts). Map zone/kind, Group owner/companions, and Travel Day transfer/buffer/document/local-time stay isolated by skin and stable leg id in `skinStates` under `rendererOwnedSkinDetails`; removing a leg or day prunes every orphaned pocket. Offline is deliberately read-only. Days-until, unbooked count, and today's plan stay in [fields/expansion.ts](../src/widgets/fields/expansion.ts), and per-skin resting faces live in [trip.ts](../src/utils/restingFaces/trip.ts): every skin folds into the shared rows grammar with its own eyebrow, lead, trailing value, and tone (booking gaps first, confirmation codes, areas, owners, leave-by times) | [27-trip-skins.css](../src/styles/product/27-trip-skins.css): rows are divider-separated regions of one backplate, `WidgetPanel` remains only as the removal-transition owner, every text-control wrapper carries `gp-bare-field`, the timeline and zone rails are flat pseudo-element lines, and container queries shed the start date, phase chip, and trailing detail columns before text truncates | [tripItinerarySkinModel.test.ts](../src/components/widgets/modules/tripItinerarySkinModel.test.ts), [tripItinerarySkins.test.ts](../src/widgets/tripItinerarySkins.test.ts), then exercise all seven skins, add/edit/remove days and legs, booked and stop-kind cycling, the person filter, narrow cards, both themes, and reduced motion |
| Atlas skins: Object, Dial, Ledger, Trend, Schedule, Compact — the shapes worn by all 50 Atlas cards (Hydration, Prayer Times, Fuel Log, Meeting Meter, Visa Runway, and the rest) and by the compatibility-only `tracker` | [AtlasWidgets.tsx](../src/components/widgets/modules/AtlasWidgets.tsx) draws all six bodies and resolves the worn skin itself, dispatched for every `ATLAS_TYPES` entry by [catalogWidgetRenderers.tsx](../src/components/widgets/renderers/catalogWidgetRenderers.tsx) | Skins are derived, never hand-listed: [atlasSkins.ts](../src/widgets/atlasSkins.ts) offers a shape only when that type's `AtlasSpec` in [atlasCatalog.ts](../src/widgets/atlasCatalog.ts) can honestly fill it — Trend needs a `series` field, Schedule needs stored times, Ledger needs a collection visual, Dial needs a writable `primary`/`target` — so a type with none still gets Object and Compact. [registry/atlas.ts](../src/widgets/registry/atlas.ts) attaches them with `skinField: 'skin'` because `mode` already carries domain meaning (a Fasting window's is `ramadan`); wearing a skin writes nothing but `skin`, which is optional in `AtlasWidgetData` ([widgetDataExpansion.ts](../src/types/widgetDataExpansion.ts)) so pre-skin boards hydrate unchanged. Every skin keeps the type's own accent — the hue says which system a card is, not which shape it wears — and all readings still come from [fields/atlas.ts](../src/widgets/fields/atlas.ts). The `tracker` skin roller is unrelated: it picks the Atlas *preset* through [widgetSkins.ts](../src/utils/widgetSkins.ts) | No new stylesheet — the bodies reuse `gp-atlas-object`, `gp-well`, `gp-subdivision`, and `gp-hairline`; search `gp-atlas` in [product.css](../src/styles/product.css) | [atlasSkins.test.ts](../src/widgets/atlasSkins.test.ts) pins applicability and the no-data-loss switch, [AtlasWidgets.test.tsx](../src/components/widgets/modules/AtlasWidgets.test.tsx) pins each body's anatomy and its empty states, then roll a card through all its skins and back and confirm the readouts, actions, and history are unchanged |
| Demo boards — the prepared `.grovepad` packages used for the launch film and screenshots | [buildDemoBoards.ts](../scripts/demo/buildDemoBoards.ts), run with `npm run demo:boards`; output and its notes live in [demo-boards/](../demo-boards) | Layout, validation, and glue/wire/relation primitives: [demoBoardKit.ts](../scripts/demo/demoBoardKit.ts); the board itself is authored in [demoBoardContent.ts](../scripts/demo/demoBoardContent.ts). Every card, skin, port, and command is checked against the live registry and field descriptors as it is placed, and the finished package is read back through `parsePersistedBoard` before it is written. Cards whose type has no resting summary are auto-pinned so a demo board never shows an anonymous icon square; a handful of renderers measure taller than `computeDataHeight` predicts and carry an explicit measured `size` | N/A — the generator writes board data, never styles | `npm run demo:boards`, then import the package and confirm no card resizes or moves on mount |
| Skin Gallery — one workspace showing every widget type wearing every skin, twice | [buildSkinGallery.ts](../scripts/skins/buildSkinGallery.ts), run with `npm run demo:skins`; output is `demo-boards/skin-gallery.json`, copied to `public/` so the running app can fetch it; the in-app path is Settings → Data → “Load Skin Gallery”, owned by [skinGalleryLoad.ts](../src/utils/skinGalleryLoad.ts); [injectSkinGallery.js](../scripts/skins/injectSkinGallery.js) is the equivalent console paste | Sample content per widget family lives in [skinGallerySamples.ts](../scripts/skins/skinGallerySamples.ts): the Atlas envelope, the automation envelope, and one hand-authored patch per remaining type, applied AFTER `dataWearingSkin` so the sample always wins. Each canvas carries a pinned open card and a resting-face card for every skin `skinsFor` reports. The fragment is merged into a live board through `loadBoard` (import drops a package onto the current canvas, which would not be a separate workspace); the skin-pair layout on each type's own canvas reserves generous slack because several renderers paint past the box `computeDataHeight` predicts, while the index grid of canvas doors packs one grid cell apart — a door never overruns its box. The loader merges rather than replaces — every record whose id lacks the `skinlab-` prefix is carried across, and the merged board is validated by `parsePersistedBoard` before it reaches `loadBoard` | N/A — the generator writes board data, never styles | `npm run demo:skins`, the settings contract test, then press the button and confirm the existing workspaces survive and no two cards collide on the dense canvases (Tasks, Invoices, Tracker) |
| Recipes — the 284-template catalogue, shelf browsing, building a whole board from one template | [RecipesModal.tsx](../src/components/ui/RecipesModal.tsx), opened by the toolbar Recipes button in [CanvasToolbar.tsx](../src/components/ui/CanvasToolbar.tsx) via the `recipesOpen` flag in [uiLinkingSlice.ts](../src/store/slices/uiLinkingSlice.ts) | Data is generated, never hand-edited: [recipeData.ts](../src/utils/recipes/recipeData.ts) is rebuilt from `docs/Grovepad-Template-Catalogue.docx` by [build-recipes.py](../scripts/build-recipes.py); shape lives in [recipeTypes.ts](../src/utils/recipes/recipeTypes.ts). [createRecipe.ts](../src/utils/recipes/createRecipe.ts) owns construction — it snapshots history once and suppresses the rest, so a whole board is one undo step, and it routes every mutation through public store actions (`createWidget`, `dataWearingSkin` + `updateWidgetData`, `addRelation`, `addConnection`) rather than writing state itself. Slot coordinates are an opening arrangement; the collision solver still places cards | Reuses the widget-library surface classes — search `gp-widget-picker` and `gp-picker-` in [03-glass-chrome.css](../src/styles/product/03-glass-chrome.css) | [recipeCatalogue.test.ts](../src/utils/recipes/recipeCatalogue.test.ts) re-validates all 284 recipes against the live registry, field descriptors, and command descriptors — run it after any registry, skin, or port change, then `npm run check` |
| Add-widget picker, catalogue grouping, widget defaults, favorites | [AddWidgetModal.tsx](../src/components/ui/AddWidgetModal.tsx) | [registry.ts](../src/widgets/registry.ts) and registry families for catalogue data; device-local favorite prefs (not board state) in [useWidgetPickerPrefsStore.ts](../src/store/useWidgetPickerPrefsStore.ts). The picker only READS `activePacks` to filter its grid — it owns no route into pack management, because adding a widget is a hot path and choosing which libraries exist is a setup decision taken once | Search `gp-widget-picker` in [product.css](../src/styles/product.css) | Build, lint, picker keyboard/pointer smoke |
| Domain packs — which specialist libraries the board offers, and per-pack widget curation | [DomainPackSettings.tsx](../src/components/ui/DomainPackSettings.tsx), rendered by the Data section of [SettingsPanel.tsx](../src/components/ui/SettingsPanel.tsx) | Pack membership is data: [modulePacks.ts](../src/types/modulePacks.ts) maps each widget type to at most one `DomainPack`, applied onto the registry in [registry.ts](../src/widgets/registry.ts). Which packs a board has switched on is BOARD state (`activePacks`, `togglePack` in [uiLinkingSlice.ts](../src/store/slices/uiLinkingSlice.ts), persisted and merged on import); which widgets INSIDE an active pack are hidden is DEVICE state (`hiddenPackWidgetTypes` in [useWidgetPickerPrefsStore.ts](../src/store/useWidgetPickerPrefsStore.ts), localStorage). The Command Palette's `action-domain-packs` opens Settings on `data` rather than any board surface | Settings island material in [03-glass-chrome.css](../src/styles/product/03-glass-chrome.css) (`gp-settings-canvas-card`) | [settingsContracts.test.ts](../src/components/ui/settingsContracts.test.ts), then toggle a pack and confirm the picker's grid and count change |
| Light/dark mode, glass, global widget color system | [useThemeStore.ts](../src/store/useThemeStore.ts), [index.css](../src/index.css), [product.css](../src/styles/product.css) | CSS theme tokens and registry accents. **The canvas colour is tokens-only, in both themes**: [CanvasViewport.tsx](../src/components/canvas/CanvasViewport.tsx) paints one `color-mix(in srgb, var(--gp-canvas-tint-base) var(--gp-canvas-tint-strength), <workspace tint>)` with no theme branch, and each theme states its own base and tint share in [01-tokens-base.css](../src/styles/product/01-tokens-base.css) — dark near-black at 97%, light sage paper `#eff4ea` at 92% (a LOWER strength is a STRONGER tint, since it is the base's own share of the mix). A hardcoded per-theme literal in the component silently overrides every token and tuning value beneath it, which is exactly how the light board became unchangeably white. The light base is stated twice by necessity — the stylesheet and `DEFAULT_CANVAS_COLORS.light` in [auraTuning.ts](../src/components/canvas/auraTuning.ts), which the tuning panel edits — so both move together, pinned by [lightThemeSurface.test.ts](../src/styles/lightThemeSurface.test.ts) and [auraTuning.test.ts](../src/components/canvas/auraTuning.test.ts). The light theme's inverted neutral scale must keep real chroma (~0.02–0.03): below that the "sage" is under the eye's threshold and every surface collapses to grey-white | [glass constitution](widget-glass-constitution.md) | `npm run check:full` plus both-theme visual smoke |
| Local calendar dates, relative-day defaults, due-date seeds | [localDate.ts](../src/utils/localDate.ts), then search `localDayKey` consumers | The local calendar helper owns `YYYY-MM-DD` day keys; registries and field/default producers own domain offsets | Date inputs stay local; never derive a day key with UTC `toISOString()` | [localDate.test.ts](../src/utils/localDate.test.ts), typecheck, positive-offset browser smoke |
| Board mutations, undo/redo, glue, hierarchy, untangle, auto-scale, overlap check | [useWidgetStore.ts](../src/store/useWidgetStore.ts), then the named domain in [slices](../src/store/slices) | [widgetStoreTypes.ts](../src/store/widgetStoreTypes.ts); action bodies live in domain slices. `glueWidgets` rides an in-flight drag's undo step; every non-drag glue (⌘G, Quick Add, the selection bar) goes through `glueSelection`, which opens its own step. **Every committed geometry change runs the overlap check** ([widgetSettling.ts](../src/store/widgetSettling.ts)): resize (snapped only), edge-resize, scale-state swap, grid snap, rename (a resting tile is measured from its title), pin/unpin, drag-reflow commit, and glue commit. Live gesture frames (`moveWidget`, `resizeWidget` with `snap: false`) deliberately do NOT settle — the release does, or settling would shove neighbours under the pointer. The acting widget is passed as `anchorIds`, so it holds its ground (and is not re-snapped) while the board makes room around it. Undo/redo and `loadBoard` never settle: they must restore exactly what was recorded | [architecture map](architecture-map.md) invariants | [overlapCheck.test.ts](../src/store/overlapCheck.test.ts), [widgetSettling.test.ts](../src/store/widgetSettling.test.ts), then `npm run check` |
| Glue welds (0.3-cell carved gaps), option-drag glue/unglue gesture, cluster drag-all, bond preview | [WidgetCard.tsx](../src/components/widgets/WidgetCard.tsx), [GlueClusterChrome.tsx](../src/components/widgets/GlueClusterChrome.tsx) | There is NO backplate and no group object: glued widgets stay individual cards welded edge-to-edge at a `GLUE_GAP` (0.3-cell, 12px) seam. Bond/seam/snap geometry is one owner: [glueGeometry.ts](../src/utils/glueGeometry.ts) (`findGlueSnap` picks the nearest facing widget within `GLUE_RANGE` = one cell, lands at exactly `GLUE_GAP` on the bond axis and grid-snapped on the perpendicular axis; nothing is painted into the carved gap: the gradient weld seam is repealed entirely, and a cluster reads as one object through its members' own local aura pooling on the canvas behind them — see the ambient aura route). Rigid settle anchors on the member closest to the grid, so welding never moves untouched clustermates; an ⌥-drag never arms drag reflow. Cluster membership (`glues`, `widgetGlueIndex`) and the actions (`glueWidgets`, `unglueWidget`, `commitGlue`) live in [glueSlice.ts](../src/store/slices/glueSlice.ts); ephemeral option-drag intent (`glueIntent`, `unglueIntentWidgetId`) previews the exact bond a release would commit — and a release that welds NOTHING pulls the member off (WidgetCard's pointer-up), so a card can never end up half-out: framed with a group it no longer touches, parked off-grid. Membership carries no adjacency, so `reconcileGlueClusters` (glueGeometry.ts, using `connectedGlueComponents`, edge = within `GLUE_RANGE`) re-derives clusters from what actually touches after every commit/unglue/delete, at every settle release (`settleWidgets`), after a member's scale-state change, and at board load (`loadBoard`, skipping records that carry unknown newer-version fields, which round-trip untouched) — a member re-welded elsewhere, a deleted connector, or any stale persisted record never leaves a cluster whose pieces float apart yet drag as one. A plain drag of any member moves the whole cluster (`moveWidget` expands through clusters; `soloGlued` is the option-drag exception) and `selectWidget` selects the whole cluster to match. **A RIGHT-CLICK is the one exception**: WidgetCard's `onContextMenu` narrows to the pressed card through `selectWidgets([widgetId])` — the menu acts on the selection (`actionIds` in [WidgetContextMenu.tsx](../src/components/ui/WidgetContextMenu.tsx)), so the cluster-expanding select made Duplicate and Delete group-wide with no way to reach one member. It re-aims even when the cluster is ALREADY the selection (`selectionIsWholeCluster`), which is the normal state once you have clicked a member; it leaves a deliberate multi-widget selection (marquee, shift-click) alone; and a FOLDED cluster keeps the collection, since one object has no individual card to aim at. Group-wide commands are the group frame's own static buttons, not the menu. Settling and reflow treat a cluster as one rigid unit whose internal seams survive every snap ([widgetSettling.ts](../src/store/widgetSettling.ts) rigid cluster snap). `findGlueSnap` forgives a half-cell overshoot (`GLUE_OVERSHOOT`) so dragging a card onto a neighbour still welds; the weld target lights up via `[data-glue-target]`. Grouped members are stored grid-aligned and touching; each renders inset by `GLUE_HALF_GAP` (`glueMemberInsets`, applied in [WidgetCard.tsx](../src/components/widgets/WidgetCard.tsx)) so the seam is carved equally and the cluster's corners hold the grid. A cluster wears a group frame ([GlueClusterChrome.tsx](../src/components/widgets/GlueClusterChrome.tsx)): unbroken top/bottom boundary lines set `GLUE_FRAME_BAND` (0.5 cell) off the cards, and a title row with a widget's own anatomy — a tinted group-icon square, the bold editable name (`WidgetGlue.name`), then the SAME STATIC action buttons a widget's name row carries (Complete-all when a checklist member is aboard, Favorite-all, Delete-group — one history step each through `updateWidgetsMetadata`; there is NO customize menu anywhere, on cards or groups), then the group's own Collapse/expand (`setClusterCollapsed`) and Ungroup (`unglueCluster`) buttons; no pill. Ungroup pushes the members a clear cell apart (`spreadClusterMembers`) so the split is physical, while LOSING a member does the opposite — whether it was deleted or dragged out (`unglueWidget`), the survivors close ranks and re-weld (`closeClusterGaps`; a folded block re-stacks) BEFORE membership is re-derived, so the widgets standing beyond a departing card are never ungrouped by someone else's exit. Both unglue paths also reset the selection (selecting a member selects the whole cluster, so a freed widget would otherwise drag its former group across the board on the next plain drag). Glued members KEEP their own title rows — the frame names the group, not the cards inside it. **Chrome is reserved space, and the group's boundary is TWO rects, never one grown box.** A widget's own floating title row (`WIDGET_TITLE_ROW`, one cell above its box) is occupied space whenever it is actually shown — `widgetShowsTitleRow` in [widgetRest.ts](../src/utils/widgetRest.ts) mirrors WidgetCard's rule (only an icon and an icon-faced resting tile float nothing; membership is NOT an exception, since hiding the row silently turned that strip into free space a clustermate was packed into). `glueChromeRect` applies it, so the cluster reflow, `closeClusterGaps`, and board settling all pack cards without painting one member's name over the card above it. `clusterChromeEnvelope` is the single envelope every cluster-shaped thing is drawn or anchored from — including [GlueClusterChrome.tsx](../src/components/widgets/GlueClusterChrome.tsx)'s own boundary lines, which used to come off a second, bare-box envelope and so cut across the top member's title. The group itself is `clusterFrameEnvelope` (member chrome + `GLUE_FRAME_BAND` on every side — this is what keeps an outside widget from settling flush against the frame) UNION `clusterTitleRowRect` (the name row bounded to its icon + label + buttons, sitting `GLUE_TITLE_HEADROOM` above the members' own rows). The empty canvas beside a short group name stays ordinary board: a cmd-drag relation hits the frame or the row ([linkTarget.ts](../src/utils/linkTarget.ts) tests both), never the gap between them. Board settling still reserves the band as a bounding box (`rectFor`), the one place a rect is required. A cluster is ONE link node: [RelationLines.tsx](../src/components/canvas/RelationLines.tsx) resolves every member through `linkNodeId`, anchors to `clusterFrameEnvelope`, and dodges `clusterTitleRowRect` as a pill — so a line keeps its gap from the frame and never cuts through the group's name, two relations into one cluster merge to a single edge, and a relation wholly inside a cluster draws nothing. Collapsing folds every member to a **single grid cell** (`COLLAPSED_MEMBER_SIZE`) and stacks them into the closest square by the SAME rule the ghost tree uses for a multi-widget node (`collapsedClusterLayout` → `balancedRowCounts`, exported from [ghostTreePresentation.ts](../src/utils/ghostTreePresentation.ts)); each member's pre-fold position/size/scale state is recorded in `WidgetGlue.restore` (validated on hydration) and expanding puts every member back translated by however far the block was dragged while folded (`WidgetGlue.foldedAt` vs the block's current top-left), so the group opens around where it sits now. A folded cluster is ONE object and one pointer target: a single click anywhere on it unfolds it (`unfoldCollapsedCluster` in WidgetCard), and its members go inert — `inFoldedCluster` feeds `hoverInert`, kills the per-icon lift/bloom/resize edge/port rail, blocks ⌥-unglue, and sets `[data-cluster-collapsed]`. That single cell is the one carve-out from the 2×2 icon floor (see [spatial.ts](../src/types/spatial.ts) `ICON_MIN_EDGE`): the floor protects icons you aim at, and nothing inside a folded cluster is aimed at. A member whose footprint changes INSIDE a cluster (pin, scale swap, rename) re-packs the cluster through `reflowWeldedCluster` (glueGeometry.ts, run from [widgetSettling.ts](../src/store/widgetSettling.ts) before board-level collision): clustermates are pushed exactly far enough to touch again, along the axis they are already arranged on, so the welds and seams survive. A member that SHRINKS (scale change in `setWidgetScaleState`, unpin in `toggleWidgetPinned`, content-fit convergence or inward edge drag in `resizeWidget`) leaves a hole instead, and the cluster pulls back together (`closeClusterGaps`, which first slides any piece that drifted diagonally clear back into reach, then runs `compactWeldedCluster` — the exact mirror of the push half: free members gravitate toward the anchored card one axis at a time, nearest first, each stopping the moment it meets something, so an INTERIOR hole closes. Connectivity alone cannot see one — pull the middle card out of a block and every survivor still touches its neighbours around the gap, so the cluster never splits and a component-level repair finds nothing to do. A gap no wider than `GLUE_GAP` IS the weld and is never closed: a gesture-welded cluster is stored exactly a seam apart while a folded one is stored touching, and the carved insets make the two read identically, so tightening either would walk the group in by a seam on every footprint change). **A welded member is ANCHORED, never re-centred**: it holds the corner it was welded at while clustermates give way or close ranks around it, which is what makes opening and closing a card inside a group exactly reversible — re-centring walked the card, and the group with it, by half the size difference on every open and close. Free cards on the open board still re-centre. Folding is ONE owner — `refoldCollapsedCluster` (glueGeometry.ts) — shared by the first fold, by a widget welded INTO a collapsed group, and by a re-pack after a member leaves: members already folded keep their recorded pre-fold state, a newcomer has its current state recorded now, and the block anchors on the standing members so a merge re-stacks in place. `glueWidgets` carries `collapsed` + `restore` + `name` across a merge (a weld touching a collapsed group stays collapsed and folds the newcomer in — losing that flag was the bug where an added widget "forgot" to join and unfold left orphans). Every path that can release a folded member — `unglueWidget`, `unglueCluster`, `deleteWidgets`, and reconcile splits in `commitGlue` — runs `unfoldReleasedFoldedMembers` (glueGeometry.ts), which restores any widget left at the 1×1 `COLLAPSED_MEMBER_SIZE` outside a collapsed cluster (from the pre-change restore map, else its dormant size), so a released member is never stranded below the aim-at floor. | Glue + group-frame laws in [glass constitution](widget-glass-constitution.md); `.gp-group-line`, `.gp-group-bar`, `.gp-group-icon`, `.gp-group-btn`, `[data-glue-target]`, `[data-unglue-intent]` in [04-controls.css](../src/styles/product/04-controls.css) | [glueGeometry.test.ts](../src/utils/glueGeometry.test.ts), [glueSlice.test.ts](../src/store/glueSlice.test.ts), [glueChromeContracts.test.ts](../src/components/widgets/glueChromeContracts.test.ts), glued-card browser smoke |
| Destructive widget/canvas deletion, cascade disclosure, subtree Undo | [WidgetDeletionDialog.tsx](../src/components/ui/WidgetDeletionDialog.tsx), [useWidgetDeletionDialogStore.ts](../src/store/useWidgetDeletionDialogStore.ts) | Canonical blast-radius analysis: [widgetDeletion.ts](../src/store/widgetDeletion.ts); deletion transaction: [selectionSlice.ts](../src/store/slices/selectionSlice.ts) | Confirmation and toast copy locally | Deletion-impact store test plus dialog browser smoke |
| Relations, free-form relation editing | [RelationLines.tsx](../src/components/canvas/RelationLines.tsx), touch-safe commands in [SelectionActionBar.tsx](../src/components/ui/SelectionActionBar.tsx) | Relation records and target-picking state in [useWidgetStore.ts](../src/store/useWidgetStore.ts). All canvases are free-form: relations keep the drawn direction and attach on the full border, and parent links are HARD by default — drawing one makes the parent carry the child, with nothing to switch on. `WidgetMetadata.strictHold` is the opt-OUT, toggled via `updateWidgetsMetadata` from exactly one surface — the widget's right-click menu ([WidgetContextMenu.tsx](../src/components/ui/WidgetContextMenu.tsx), the Magnet/Waypoints row); the relation line's menu ([RelationLines.tsx](../src/components/canvas/RelationLines.tsx)) must never carry the switch, because the hold belongs to the parent node and reaches every parent-linked descendant, not to the one edge that was clicked. That menu asks the owner `resolveStrictHold` for the RESOLVED answer (never the raw flag, which is absent on almost every node): the nearest explicit answer at-or-above a node decides for it, so releasing a node relaxes its whole branch and a node inside a released branch can re-hold its own; the row always switches and only names where an inherited release was decided, and it is hidden entirely for a node with no parent-linked children, since there is no family to hold. Derivation (`resolveStrictHold`, `strictCarrierIds`) and the one shared moves-together closure `expandMovedWidgetIds` (glue clusters ∪ strict families, fixpoint, cycle-safe, same-canvas only) live in [widgetGraph.ts](../src/store/widgetGraph.ts) and are consumed by `moveWidget`/`settleWidgets` ([widgetLayoutSlice.ts](../src/store/slices/widgetLayoutSlice.ts)) and the reflow preview in [WidgetCard.tsx](../src/components/widgets/WidgetCard.tsx); a locked node carries nobody (it cannot move, so its family would walk off without it), and ⌥-drag (`soloGlued`) breaks every coupling for that drag. Height is the other half of the parenting law and lives in [generationFloor.ts](../src/store/generationFloor.ts) — see the layout route. Strict parent edges paint one step heavier (`gp-edge-strict`). Law: strict-hold law in [glass constitution](widget-glass-constitution.md). The old canvas-wide `relationStrict` field is tolerated compatibility data in persistence/sync only; no runtime code reads it. | Geometry: [edgeRoute.ts](../src/utils/edgeRoute.ts) — the ONE owner of where a card-to-card line touches a widget and how it travels, shared with the dependency layer. Three laws: every end stops `LINE_STANDOFF` (0.3 cell, 12px) clear of the card; each end slides along its own border to the closest point on a side that actually FACES the other card (a line never sprouts from a fixed centre, and `pairAnchors` puts both ends on ONE shared coordinate whenever their landing ranges overlap, so a child under its parent draws a dead-straight line rather than a lean); and a route is a SINGLE cubic tangent to both borders — no escape hop, no middle section, therefore no join that can kink or loop. `EdgeAttach` carries the strict-hold reading: a strict parent edge passes `{ from: 'lower', to: 'upper' }`, restricting the parent to the bottom half of its border and the child to its upper half, still at the closest points those halves allow. Landing points are held back from the rounded corners (`CORNER_INSET`) and slid clear of a floating name capsule along their own border (`dodgePill`), stepping outward past it only when the capsule covers the whole edge. `routeEdgeToPoint` gives the cmd-drag preview the same border exit. Circuit wires are NOT here — [curve.ts](../src/utils/curve.ts) keeps `flowCurve` for port-to-port traces only; target arbitration: [widgetPointerPolicy.ts](../src/utils/widgetPointerPolicy.ts); shared paint: [CanvasEdge.tsx](../src/components/canvas/CanvasEdge.tsx) | Pointer-policy, shared-edge/curve tests, [strictHold.test.ts](../src/store/strictHold.test.ts), free-form relation smoke |
| Dependencies, blocker direction, dependency chips | [DependencyLines.tsx](../src/components/canvas/DependencyLines.tsx) | Blocker relations in [useWidgetStore.ts](../src/store/useWidgetStore.ts) | Geometry: [dependencyGeometry.ts](../src/utils/dependencyGeometry.ts) owns only the RAILS — a dependency always leaves the prerequisite's right edge and enters the dependent's left one, sliding on y toward the other card — and takes its 0.3-cell gap from `LINE_STANDOFF`, the same constant relations use. The path itself is [edgeRoute.ts](../src/utils/edgeRoute.ts) (`edgePath`/`edgeMidpoint` with fixed `'right' → 'left'` sides), so a dependency and a relation between the same two cards curve identically; shared paint: [CanvasEdge.tsx](../src/components/canvas/CanvasEdge.tsx) | Shared-edge and dependency geometry tests plus dependency smoke |
| Input/output ports, wires, connection propagation | [PortRail.tsx](../src/components/widgets/PortRail.tsx), [WireLayer.tsx](../src/components/canvas/WireLayer.tsx), Connect mode in [CanvasModeDock.tsx](../src/components/ui/CanvasModeDock.tsx) | [fields.ts](../src/widgets/fields.ts), [circuitEngine.ts](../src/engine/circuitEngine.ts), [useCircuitStore.ts](../src/store/useCircuitStore.ts) | Tap/drag slop: [pointerTap.ts](../src/utils/pointerTap.ts); geometry: [portGeometry.ts](../src/utils/portGeometry.ts); paint: [CanvasEdge.tsx](../src/components/canvas/CanvasEdge.tsx); [circuit-engine.md](circuit-engine.md) | Pointer-tap, circuit-store, shared-edge, connection lifecycle tests plus tap-to-connect/drag-wire smoke |
| Persistence, visible save/offline state, installable offline shell, device state, reload, migration snapshots, IndexedDB, cloud sync, stale deploys | [persistence.ts](../src/utils/persistence.ts), [AccountChip.tsx](../src/components/ui/AccountChip.tsx) | Status wording: [persistenceStatus.ts](../src/utils/persistenceStatus.ts); network/service-worker lifecycle: [networkStatusRuntime.ts](../src/runtime/networkStatusRuntime.ts); web manifest and cache policy: [manifest.webmanifest](../public/manifest.webmanifest), [service-worker.js](../public/service-worker.js); board schema: [persistedBoardSchema.ts](../src/utils/persistedBoardSchema.ts); local navigation: [persistedDeviceState.ts](../src/utils/persistedDeviceState.ts); protected snapshot replacement/rollback: [snapshotRestore.ts](../src/utils/snapshotRestore.ts); cloud reconciliation: [cloudSync.ts](../src/utils/cloudSync.ts) (`fetchCloudHead` settles most reconciles from two checksum columns, no board transferred); what the two sides last agreed on: [syncBaseline.ts](../src/utils/syncBaseline.ts); silent three-way merge that replaced the conflict prompt: [boardThreeWayMerge.ts](../src/utils/boardThreeWayMerge.ts); id-remapping workspace append used by board imports: [boardWorkspaceMerge.ts](../src/utils/boardWorkspaceMerge.ts); portable `.grovepad` package: [grovepadPackage.ts](../src/utils/grovepadPackage.ts); IndexedDB: [boardDatabase.ts](../src/utils/boardDatabase.ts); deploy monitor: [deployVersionMonitor.ts](../src/runtime/deployVersionMonitor.ts) | [Storage format plan](storage-format-plan.md) and persistence flow in [architecture map](architecture-map.md) | status/runtime tests, board/device/cloud tests, production build, manifest/service-worker fetch, online/offline/reload smoke |
| Link previews, browser-tab page title/description, crawler rules, canonical URL, app icons, Sites deployment output | Static shell metadata: [index.html](../index.html); runtime page titles: [App.tsx](../src/App.tsx), [TimerTitleRuntime.tsx](../src/components/canvas/TimerTitleRuntime.tsx), formatted by [pageTitle.ts](../src/utils/pageTitle.ts) | Preview card art: [og-image.png](../public/og-image.png) (1200×630, regenerated by hand, not built); crawler rules: [robots.txt](../public/robots.txt), [sitemap.xml](../public/sitemap.xml); cache/security headers: [_headers](../public/_headers); the Clash Display faces are vendored in [public/fonts/](../public/fonts/) and declared in [index.css](../src/index.css) — never fetched from Fontshare at runtime, because a remote face breaks the offline claim and reflows every label a beat after launch; each filename carries the `font-weight` it holds, they are precached by [service-worker.js](../public/service-worker.js) (bump its cache name when they change) and pinned by [globalTypography.test.ts](../src/styles/globalTypography.test.ts); favicon/manifest/apple-touch icons: [favicon.png](../public/favicon.png) is the 64px rounded green tile with the mark drawn larger for tab size, linked once from [index.html](../index.html) and [404.html](../public/404.html) (it carries its own plate, so the old light/dark pair keyed to browser chrome is gone) and precached by [service-worker.js](../public/service-worker.js); [app-icon-192.png](../public/app-icon-192.png)/[app-icon-512.png](../public/app-icon-512.png) (rounded green tile, manifest `purpose: any`), `app-icon-maskable-*.png` (full-bleed, mark pulled into the 80 % safe zone, `purpose: maskable`) and [apple-touch-icon.png](../public/apple-touch-icon.png) (180px light iOS icon) are all written by the app-icon script below so installed web apps match the native app (bump the service-worker cache name when they change, they are precached); brand source assets in [public/brand/](../public/brand/) — `logo_light_borderless.png`/`logo_dark_borderless.png` are the transparent ink-only marks, carrying no squircle frame of their own (always light logo on dark surfaces, dark logo on light surfaces; `grovepad` wordmark is always lowercase), each paired with the `.pxd` it exports from, `app-icon-source.png` is the retired bitmap icon source (the light mark on transparency), kept only as a brand asset; every app icon now comes from one geometric drawing in [build-app-icons.py](../scripts/build-app-icons.py) (`python3 scripts/build-app-icons.py`, macOS only because it shells out to `iconutil`): the iOS catalog [AppIcon.appiconset](../src-tauri/gen/apple/Assets.xcassets/AppIcon.appiconset/) holds three single 1024px appearance variants — light (opaque green plate, dark `g`, white `p`, also copied to [app-icon-ios.png](../public/brand/app-icon-ios.png)), dark (transparent, white `g`, green `p`; iOS 18+ paints its own dark gradient behind it) and tinted (transparent greyscale that iOS recolours) — and Xcode derives every smaller size at build time; the script then writes the Tauri layers under [src-tauri/icon-source/](../src-tauri/icon-source/) and runs `npx tauri icon src-tauri/icon-source/manifest.json` for the Windows `.ico`, Linux PNGs, Store logos and the Android adaptive icon (plate background, pre-shrunk foreground, monochrome themed layer, wired in `mipmap-anydpi-v26/ic_launcher.xml`), purges the legacy per-size iOS files that command drops into the catalog, and builds `icon.icns` itself from the Apple-style inset tile with a shadow; never run `tauri icon` by hand against that catalog, and validate with `xcrun actool` if the catalog manifest changes; [build-sites-output.mjs](../scripts/build-sites-output.mjs) adds the static SPA worker and hosting metadata expected by Sites after Vite finishes | Single-page app: every path serves the same shell, so the canonical tag stays `https://grovepad.app/` and collaboration invite links stay disallowed | [pageTitle.test.ts](../src/utils/pageTitle.test.ts), then `npm run build`; confirm `dist/` contains `server/index.js`, `.openai/hosting.json`, `robots.txt`, `sitemap.xml`, `og-image.png` and the `og:` tags survive in `dist/index.html` |
| Multiplayer, public canvas links, live cursors, shared selection, follow mode, collaborative notes, invitations, roles, comments, reconnect | [collaborationRuntime.ts](../src/runtime/collaborationRuntime.ts), [CollaborationOverlays.tsx](../src/components/collaboration/CollaborationOverlays.tsx) | Share registration/recovery policy: [canvasSharing.ts](../src/collaboration/canvasSharing.ts); CRDT schema/validation: [yjsCanvas.ts](../src/collaboration/yjsCanvas.ts); canonical-store bridge: [canvasStoreBridge.ts](../src/collaboration/canvasStoreBridge.ts); UI state: [useCollaborationStore.ts](../src/store/useCollaborationStore.ts); follow-mode reconciliation (a departed target must release the camera, which follow holds via the gesture engine): [followTarget.ts](../src/collaboration/followTarget.ts); durable/offline transport: [supabaseCollaboration.ts](../src/collaboration/supabaseCollaboration.ts), [offlineUpdateQueue.ts](../src/collaboration/offlineUpdateQueue.ts); authorization: [realtime collaboration migration](../supabase/migrations/20260719050000_realtime_collaboration.sql), [awareness-channel migration](../supabase/migrations/20260722093000_realtime_awareness_channel.sql), and [public-link migration](../supabase/migrations/20260728120000_public_canvas_links.sql) | [Realtime collaboration runbook](realtime-collaboration.md); collaboration styles in [07-collaboration.css](../src/styles/product/07-collaboration.css) | Collaboration + pressure suites, `npm run check:full`, database policy tests, then signed-in two-browser/reconnect/role/public-link smoke |
| AI Generator widget, generated canvas output | [AiGeneratorWidget.tsx](../src/components/widgets/modules/AiGeneratorWidget.tsx) | [widgetGeneration.ts](../src/services/widgetGeneration.ts), then [localAiService.ts](../src/services/localAiService.ts) | Widget status UI locally; plan validation stays in the AI service | [widgetGeneration.test.ts](../src/services/widgetGeneration.test.ts), local-AI tests, generation/cancel smoke |
| Panel/list removal animation or copied acknowledgement | [WidgetPanel.tsx](../src/components/widgets/WidgetPanel.tsx), [useTransientValue.ts](../src/hooks/useTransientValue.ts) | Stable item ids in widget data; timeout ownership in [ownedTimeout.ts](../src/utils/ownedTimeout.ts) | `gp-subpanel-exit` in [product.css](../src/styles/product.css) | Panel-removal and owned-timeout tests plus reduced-motion smoke |
| Quick Add, thought interpretation, scenario prediction, blueprint preview, command line (verbs over the selection) | [QuickAddSheet.tsx](../src/components/ui/QuickAddSheet.tsx), [QuickAddPreviewLayer.tsx](../src/components/canvas/QuickAddPreviewLayer.tsx) | [thoughtInterpreter.ts](../src/utils/thoughtInterpreter.ts), [scenarioResolver.ts](../src/utils/scenarioResolver.ts); command grammar/registry: [commandLine.ts](../src/utils/commandLine.ts), dispatch onto store actions: [commandExecutor.ts](../src/utils/commandExecutor.ts); shared parent-graph layout: [planLayout.ts](../src/utils/planLayout.ts); pure preview scene: [quickAddPreviewScene.ts](../src/components/canvas/quickAddPreviewScene.ts) | [scenario-intelligence.md](scenario-intelligence.md); candidate command vocabulary: [quick-add-command-lexicon.md](quick-add-command-lexicon.md) | Scenario/scaffold/structural planner and preview-contract tests |
| Command palette, global search (titles, body text, canvases), recent canvases, find-prefill | [CommandPalette.tsx](../src/components/ui/CommandPalette.tsx) | `searchWidgets`/`openPaletteSearch` in [uiLinkingSlice.ts](../src/store/slices/uiLinkingSlice.ts); content flattening and match excerpts: [widgetSearchText.ts](../src/utils/widgetSearchText.ts); shared jump-to-result (palette + `go` verb): [searchLeap.ts](../src/utils/searchLeap.ts); device-local visit trail (recent-canvas rows, workspace re-entry landing): [canvasRecents.ts](../src/store/canvasRecents.ts) | Palette classes `gp-command-*` in [product.css](../src/styles/product.css) | [CommandPalette.test.ts](../src/components/ui/CommandPalette.test.ts) and [searchAndRecents.test.ts](../src/store/searchAndRecents.test.ts) |
| Optional local AI, model profiles, deterministic/model handshake | [localAiService.ts](../src/services/localAiService.ts), [local-ai](../src/services/local-ai) | [planProtocol.ts](../src/services/local-ai/planProtocol.ts), runtime adapters | [local-ai-runtime.md](local-ai-runtime.md) | Local-AI, plan protocol, profile, and adapter tests |
| AI plugin, MCP, external mind-map/tree generation, bring-your-own AI account | [grovepad-mcp.mjs](../scripts/grovepad-mcp.mjs), [mcpBridgeRuntime.ts](../src/runtime/mcpBridgeRuntime.ts) | Preview lifecycle and consume-once commit: [useMcpConnectorStore.ts](../src/store/useMcpConnectorStore.ts); on-canvas review pill: [McpPreviewLayer.tsx](../src/components/canvas/McpPreviewLayer.tsx); browser validation and tree plans: [treeContract.ts](../src/mcp/treeContract.ts); loopback broker: [grovepadBridge.mjs](../scripts/mcp/grovepadBridge.mjs); opt-in: [useSettingsStore.ts](../src/store/useSettingsStore.ts) | [Grovepad MCP connector](mcp-connector.md); only the open app may call canonical board actions | MCP contract, preview-lifecycle, and bridge tests, typecheck, lint, then connect/preview/commit/Undo smoke |
| Grovepad inside AI chatbots (remote MCP + MCP Apps), widget plans an AI may create | [widgetPlanContract.ts](../src/mcp/widgetPlanContract.ts) | Curated 15-type allowlist, bounds, wire validation against the live field registry, and the pure server-side apply onto a [CloudCanvasDocument](../src/utils/cloudDocuments.ts); JSON Schema for MCP tool inputs lives beside the normalizer and is drift-tested | [Remote MCP apps plan](remote-mcp-apps-plan.md), [Phase 0 findings](remote-mcp-phase0-findings.md); widget data always starts from the registry's own `defaultData()` so a model can never inject a data shape | `npm run check` (the contract is pure — no network, no store) |
| Widget layer virtualization, gradual hydration, far-zoom image previews, ambient aura | [WidgetLayer.tsx](../src/components/widgets/WidgetLayer.tsx), [CanvasAuraLayer.tsx](../src/components/canvas/CanvasAuraLayer.tsx) | Camera-window math and the 20% resting-face interaction boundary: [widgetVirtualization.ts](../src/utils/widgetVirtualization.ts); centre-first idle batching: [useProgressiveWidgetMounts.ts](../src/hooks/useProgressiveWidgetMounts.ts); retained camera window with pan hysteresis: [useVirtualCanvasWindow.ts](../src/hooks/useVirtualCanvasWindow.ts); one-image placeholder and bounded SVG-data cache: [LightweightWidgetPreview.tsx](../src/components/widgets/LightweightWidgetPreview.tsx), [widgetPreviewImage.ts](../src/utils/widgetPreviewImage.ts). Widgets outside the retained window mount nothing; nearby widgets first mount one lightweight image, then hydrate to a full [WidgetCard.tsx](../src/components/widgets/WidgetCard.tsx) in small batches. Below 20% zoom, inactive widgets stay as images and resting faces are pointer-transparent; selected/expanded widgets remain urgent and live. The parent world transform still owns all per-frame camera work, and React only replans after the camera consumes its overscan gutter. The aura layer redraws from the camera and widget-store mirrors into an adaptive, viewport-aspect buffer. Each on-screen widget emits from its actual resting/expanded footprint, stays centred during movement, and wears a monotonic elliptical falloff whose screen-space halo changes only mildly with zoom. Glued members always rank into the emitter set so their local pools can still join a cluster without reviving the removed painted weld seam | Global reduced-motion fail-safe in [index.css](../src/index.css) | [widgetVirtualization.test.ts](../src/utils/widgetVirtualization.test.ts), [widgetPreviewImage.test.ts](../src/utils/widgetPreviewImage.test.ts), [auraTuning.test.ts](../src/components/canvas/auraTuning.test.ts), typecheck, build, low/high zoom interaction, fast pan, circuit mode and reduced-motion smoke |
| Aura tuning knobs, accent overrides, canvas colour tuning | [auraTuning.ts](../src/components/canvas/auraTuning.ts) owns every number and the sanitizer; [useAuraTuningStore.ts](../src/store/useAuraTuningStore.ts) persists a draft (the dev-only editor panel was removed; the store and document format remain) | `DEFAULT_AURA_TUNING` is the single production source. Every pasted or restored value passes `sanitizeAuraDocument`, because a non-finite radius throws out of `createRadialGradient` | Canvas colours apply as inline custom properties via `applyCanvasColors` (DEV only) and are lifted on teardown, so the stylesheet stays authoritative | [auraTuning.test.ts](../src/components/canvas/auraTuning.test.ts), then aura smoke in both themes |
| Owner-facing appearance Fine-tune menu (blur/shadow/motion knobs), `G` shortcut | Catalogue + apply/sanitize: [uiTuning.ts](../src/components/ui/uiTuning.ts); persisted local overrides: [useUiTuningStore.ts](../src/store/useUiTuningStore.ts); panel: [UiTuningPanel.tsx](../src/components/ui/UiTuningPanel.tsx); `G` binding + mount in [CanvasViewport.tsx](../src/components/canvas/CanvasViewport.tsx) | Each `TuneField` binds one slider to one CSS custom property; a knob at its default writes nothing, so the stylesheet fallback (which MUST equal the field `default`) stays authoritative. Local only — never board data, history, or sync. Add a knob = one field here + one `var(--gp-tune-…, fallback)` in [index.css](../src/index.css). Ships in the real app, not dev-gated; the panel is `position: fixed` inline because `.gp-popup-surface` forces `position: relative` over layered Tailwind utilities | [uiTuning.test.ts](../src/components/ui/uiTuning.test.ts), typecheck, then open with `G`, drag a slider, Reset, reload smoke |
| Resting faces, click-to-expand tiles, energy-light widget rest state | Pure decision/geometry: [widgetRest.ts](../src/utils/widgetRest.ts); ephemeral view state: [useWidgetRestStore.ts](../src/store/useWidgetRestStore.ts); effective-geometry hook: [useEffectiveWidget.ts](../src/hooks/useEffectiveWidget.ts) | Every registry type rests by default; `restingFace: false` is the explicit safety escape hatch. Expansion is per-viewer view state — never history, persistence, or sync — and is centre-anchored (`restExpansionOffset`), so geometry consumers (edge layers, [PortRail.tsx](../src/components/widgets/PortRail.tsx)) must anchor to `widgetWithEffectiveSize`, not `effectiveWidgetSize` alone. An expansion remembers the state it opened from (`expandedFrom`: rest tile, or an icon's exact square) and returns to it on collapse. **Opening an icon is as ephemeral as opening a tile: the record stays `iconified` at its own square for the whole peek**, so the geometry layer sizes the open card from what the icon remembers (`expandedIconSize` = `expandedSize ?? defaultSize`), `isWidgetRestExpanded` admits a peeked icon through `iconPeeksOpen`, and `widgetWithEffectiveSize` hands consumers a card (`iconified: false`) rather than the little square underneath it. Collapse therefore writes nothing at all — no undo step, no shuffled clustermate (settling reads `restingFootprintWidget`, which still sees the icon), and nothing syncing to a shared canvas because someone glanced at a card. The swap commits at exactly one moment: a PIN (`toggleWidgetPinned` swaps `iconified`/`size` in the pin's own history step and lets the settle pass make space, and the record — still an icon at that instant — is its own witness for `pinnedFrom`). The one carve-out is a type that never rests (`restingFace: false`): it has no overlay to open into, so clicking its icon really does reopen it, as a durable undoable scale change with its neighbours making space. A peeked icon is not resizable (every resize path writes `size`, which is the icon's square until the pin) — pin first. Pinning (`metadata.pinned`, the title row's Pin button — part of the STATIC header row Pin/Favorite/Delete (+ Completed on checklists); the floating pill and the "+" customize menu are gone, and the absorbed view offset grid-snaps on pin so a pinned clustermate never drags its group off rhythm) is the opposite of resting: a durable board property that holds a card open at its stored footprint, so a pinned widget is neither resting nor the expanded member. A pin also RECORDS what it interrupted (`metadata.pinnedFrom`, a `PinOrigin` handed in by the card because only the expansion still knows the icon square it opened from; sanitized on hydration in [persistedBoardSchema.ts](../src/utils/persistedBoardSchema.ts)), so unpinning returns the card to that state — an icon comes back as its own icon, re-centred on the card you were looking at, instead of every unpin dropping onto a resting tile. While a card is held open every OTHER card is background: `hoverInert` in WidgetCard withholds the bloom, the lift, the magnetic tilt, `hoveredWidgetId`, and the outline-resize affordance (`[data-hover-inert]` excludes them in index.css); clicks still work, so the accordion still opens the next card | Resting face render path and expand/collapse cross-fade in [WidgetCard.tsx](../src/components/widgets/WidgetCard.tsx) (`.gp-rest-face`/`.gp-rest-content-in`, timed by `REST_TRANSITION_MS`); the expanded card floats one past the board's live top z with `.gp-rest-halo`, a three-cell backdrop-blur floor shadow over its neighbours (index.css); collapse wiring (Esc, canvas click) in [CanvasViewport.tsx](../src/components/canvas/CanvasViewport.tsx) | [widgetRest.test.ts](../src/utils/widgetRest.test.ts), [widgetPinned.test.ts](../src/store/widgetPinned.test.ts), typecheck, then click-expand/Esc/drag and pin/reload smoke |
| Phone fullscreen widget sheet (open a widget as its own screen) | [WidgetFullscreenSheet.tsx](../src/components/widgets/WidgetFullscreenSheet.tsx), mounted once in [CanvasViewport.tsx](../src/components/canvas/CanvasViewport.tsx) | The small-screen half of click-to-expand. On a phone (`viewportClass === 'phone'`, [adaptiveInput.ts](../src/utils/adaptiveInput.ts)) a full card is wider than the screen, so [WidgetCard.tsx](../src/components/widgets/WidgetCard.tsx)'s `openAsSheet` diverts BOTH open paths (`expandFromRest`, `expandFromIcon`, and the outline's `onEdgeClick` through them) into [useWidgetSheetStore.ts](../src/store/useWidgetSheetStore.ts) instead of the in-place expansion slot. The two stores are deliberately separate: while a sheet is open the widget is still an ordinary resting tile on the board, so NO canvas geometry — edges, wires, glue seams, settling, virtualization — has a phone case to answer (see [widgetRest.ts](../src/utils/widgetRest.ts)). The sheet is view-only in the same sense expansion is: no history, no persistence, no sync, and a phone tap on an icon does not even change its scale state. Pure flight geometry, the tap/close rule, and the one shared DOM measurement (`widgetSheetOrigin`, re-read at close because the board can pan underneath an open sheet) live in [widgetSheet.ts](../src/utils/widgetSheet.ts). The flight is modelled on a phone home screen opening an app, and is four movements at once: a `clip-path` inset grown from the tile's exact rect and corner radius to the whole layer (content sits at full size from the first frame, so nothing is ever scaled or distorted); the content stage scaling up around `sheetOriginCenter` — the tapped tile's own centre, which is why the safe-area insets live on the stage's CHILDREN and never on the panel, or that origin would be offset by the notch; the board behind receding and dimming, driven by a `data-widget-sheet` flag on the document root (the adaptiveInputRuntime convention) which also withdraws the shell's pointer events for the whole glide; and a content cross-fade that arrives a beat behind the mask and leaves well ahead of it. Timing is `--gp-sheet-motion`/`--gp-ease-sheet`, derived from the user-tunable `--gp-motion-layout` so the Fine-tune Motion setting still governs it and `restGlideMs` still reads the real duration off the panel. The opening flight is armed by a forced layout read, never `requestAnimationFrame`, which a backgrounded tab throttles to a standstill. Content is the very same [WidgetRenderer](../src/components/widgets/WidgetRenderer.tsx) inside `gp-widget-card`/`gp-widget-content`, so no skin is re-implemented at a second size. Closes on the header chevron, Escape (via [useOverlayDismiss.ts](../src/hooks/useOverlayDismiss.ts), which also keeps CanvasViewport's Escape out of it), a pull-down on the header, or the widget leaving the active canvas | [33-widget-sheet.css](../src/styles/product/33-widget-sheet.css) — the panel wears the card's own material and carries `border-radius: 0` so the clip owns the corners | [widgetSheet.test.ts](../src/utils/widgetSheet.test.ts), typecheck, then a phone-width open/edit/close smoke |
| Per-skin resting face art, content-derived tile sizing | [WidgetRestingFace.tsx](../src/components/widgets/WidgetRestingFace.tsx) dispatches; the shared shapes are [RestingFaceGrammars.tsx](../src/components/widgets/RestingFaceGrammars.tsx); the canvas door's live faces are [CanvasNodeRestFace.tsx](../src/components/widgets/CanvasNodeRestFace.tsx) | The vocabulary and its bounds live in [restingFaceModel.ts](../src/utils/restingFaceModel.ts); [restingFace.ts](../src/utils/restingFace.ts) owns dispatch, measurement, and the per-widget cache — model and tile stay ONE contract, so the layout constants in `restingFace.ts` and the markup in the two components change together. A folded card keeps the shape of the skin it is wearing, so each family answers for its own skins in [restingFaces/](../src/utils/restingFaces/): [tasks.ts](../src/utils/restingFaces/tasks.ts) (13), [table.ts](../src/utils/restingFaces/table.ts) (8), [time.ts](../src/utils/restingFaces/time.ts) (11), [calendar.ts](../src/utils/restingFaces/calendar.ts) (7), [goal.ts](../src/utils/restingFaces/goal.ts), [numeric.ts](../src/utils/restingFaces/numeric.ts) (Counter/Calculator/Formula/Number Input), [text.ts](../src/utils/restingFaces/text.ts) (Bullets/Text Input/Code/Links), [visual.ts](../src/utils/restingFaces/visual.ts) (Media/Sketchpad/Canvas node), [structure.ts](../src/utils/restingFaces/structure.ts) (Location/Toggle), [atlas.ts](../src/utils/restingFaces/atlas.ts) (the Tracker’s fifty systems across its six shapes — and the same fifty as standalone types, each naming itself rather than trusting a `trackerMode` an old board may never have written), [automation.ts](../src/utils/restingFaces/automation.ts) (the automation nodes: the status island’s own words, the input, the output or the error in its place, then the records the node holds), [expansion.ts](../src/utils/restingFaces/expansion.ts) (the thirty Expansion cards — the headline reading from the card’s own Vitals strip, then its own rows, each ending in the value that type is about: an RSVP, an invoice amount, doses taken, a serving-scaled quantity), and [catalog.ts](../src/utils/restingFaces/catalog.ts) (Risk Register, Decision Matrix, SWOT, Timesheet, Inventory, Status, Process, Metrics, Mood, the study cards, Contact, Dialog, Decision, the chart skins the base face cannot serve, Unit Converter, World Clock). Every one of them reads the SAME pure skin model the open card reads, so a tile can never contradict the card. Skins the catalogue adds without a renderer of their own are dressed once in [catalogue.ts](../src/utils/restingFaces/catalogue.ts): the skin’s name as the eyebrow (only where the family left it blank), its `skinStates.details` as rows, and its `presentation` carried to the tile as paint mirroring [08-skin-presentations.css](../src/styles/product/08-skin-presentations.css). Chart geometry: [sparkline.ts](../src/utils/sparkline.ts) | The face shows real content sized to itself — rows/text/values, a board’s columns, a month’s lattice, a tape’s total, a drawing’s own ruling with the WHOLE drawn picture bbox-fit at the picture’s own ratio (the tile reshapes to it and `paperInkRatio` letterboxes the ink, so lattice snap and title floors never stretch it — sizer and PaperFace share that one function); a bare icon cell when empty; the bare image for media (ratio-locked resting resize) — never counts, never a fixed tile. Every grammar is bounded by construction (`REST_*_LIMIT`), and exactly two faces are live: the clock (time) and the canvas door (`kind: 'canvas'`, whose fixed per-skin doorplate the store-subscribed [CanvasNodeRestFace.tsx](../src/components/widgets/CanvasNodeRestFace.tsx) fills — the tile's SIZE stays pure so anchors never move with the far canvas). A card in private mode must never print at rest what the open card hides | [restingFace.test.ts](../src/utils/restingFace.test.ts), [restingSkinFaces.test.ts](../src/utils/restingFaces/restingSkinFaces.test.ts) (every essential type × every skin), [catalogueFamilyFaces.test.ts](../src/utils/restingFaces/catalogueFamilyFaces.test.ts) (the four families above, plus full Atlas and automation-node coverage), [sparkline.test.ts](../src/utils/sparkline.test.ts), then per-skin resting/expand smoke |
| Time widget; Timer, Pomodoro, Stopwatch, Deadline, World Clock, Hourglass, Intervals, Tabata, Chess Clock, Lap Timer, Multi-stage Timer; clock ring around a card | [TimekeeperWidget.tsx](../src/components/widgets/modules/TimekeeperWidget.tsx), with the reformed calendar horizon in [CountdownWidget.tsx](../src/components/widgets/modules/CountdownWidget.tsx) and city board in [WorldClockWidget.tsx](../src/components/widgets/modules/WorldClockWidget.tsx) | `TimekeeperData` owns independent saved state for every mode; pure deadline/time-zone/protocol/chess/stage validation lives in [timeSkinModel.ts](../src/components/widgets/modules/timeSkinModel.ts). The `timer`, `pomodoro`, `stopwatch`, `countdown`, and `world_clock` cards are deleted outright; a board saved with one has that record dropped on hydration (see [deletedWidgetTypes.ts](../src/widgets/deletedWidgetTypes.ts)) rather than rewritten onto a `timekeeper` skin. One dial reading for short-duration modes: [widgetClock.ts](../src/utils/widgetClock.ts) plus [useWidgetClock.ts](../src/hooks/useWidgetClock.ts); mark geometry: [squircleTicks.ts](../src/utils/squircleTicks.ts); outline overlays go through [WidgetBoundsOverlay.tsx](../src/components/widgets/WidgetBoundsOverlay.tsx) | [18-time-skins.css](../src/styles/product/18-time-skins.css). Timer/Pomodoro/Stopwatch keep the card-outline dial; deadline, world-time, protocols, chess, laps, and stages each use one purpose-built continuous surface | [timeSkinModel.test.ts](../src/components/widgets/modules/timeSkinModel.test.ts), [TimekeeperWidget.test.tsx](../src/components/widgets/modules/TimekeeperWidget.test.tsx), [timeSkins.test.ts](../src/widgets/timeSkins.test.ts), clock geometry/reading tests, then exercise all eleven modes, start/pause/reset/skip/lap/player-switch/city-add/date-edit, resting faces, both themes, and reduced motion |
| Counter skins; Tally, Clicker, Goal Counter, Up / Down, Multi-counter, Timed Rate, Resetting Period | [CounterWidget.tsx](../src/components/widgets/modules/CounterWidget.tsx) branches all seven faces over one stored number | Pure tally/goal/rate/period derivations: [counterSkinModel.ts](../src/components/widgets/modules/counterSkinModel.ts). `label`, `count`, and `step` stay canonical so a circuit reads the same value whichever face shows; the target, the named tallies, the rate window, and the banked period history each live in their own `skinStates` entry. Resetting Period rolls the finished period into history on its own when the clock crosses a boundary, and never banks an empty period | [31-counter-skins.css](../src/styles/product/31-counter-skins.css); the goal ring is one masked conic sweep and a tally gate is one element. Multi-counter rows sit on the list's single surface because 06-field-islands.css flattens any composite wrapper | [counterSkinModel.test.ts](../src/components/widgets/modules/counterSkinModel.test.ts), [counterSkins.test.ts](../src/widgets/counterSkins.test.ts), then exercise all seven skins, add/remove tallies, the rate start/stop, a period boundary, both themes, and reduced motion |
| Contact Card skins; Personal, Business, Emergency, Vendor, Relationship, Household, Care Contact; reach routes, birthday countdown, contact cadence, escalation order | [ContactWidget.tsx](../src/components/widgets/modules/ContactWidget.tsx) branches all seven faces over one stored person | Pure route/date/cadence derivations: [contactSkinModel.ts](../src/components/widgets/modules/contactSkinModel.ts). `name`, `role`, `email`, `phone`, `organization`, `address`, `website`, `note`, `birthday`, `reference`, and `preferred` are canonical on `ContactData`, so the number typed on the Business card is the number Emergency dials; only the contact cadence, the household roster, and the care availability/escalation live in their own `skinStates` entry. `telHref`/`mailHref`/`siteHref` refuse anything that is not a dialable number, a plausible address, or an http(s) URL | [32-contact-skins.css](../src/styles/product/32-contact-skins.css); the vendor plot is a dot grid and a pin, not a fetched tile. Buttons are ghost controls under 06-field-islands.css, so any fill is painted on `.gp-contact-fill::before` and edges are drawn with `outline`; a household row being removed restores its own `opacity` transition, which is what finalizes the removal | [contactSkinModel.test.ts](../src/components/widgets/modules/contactSkinModel.test.ts), [contactSkins.test.ts](../src/widgets/contactSkins.test.ts), then exercise all seven skins, the channel toggle, a logged contact, add/remove a housemate, the escalation stepper, both themes, and reduced motion |
| World Clock skins; City Grid, Analog Wall, Overlap Band, Meeting Planner, Travel Clock, Sunlight; zone offsets, working-hour overlap, daylight arcs | [WorldClockWidget.tsx](../src/components/widgets/modules/WorldClockWidget.tsx) branches all six views over one stored zone list | Pure zone arithmetic, band/overlap/meeting/travel/sun derivations: [worldClockSkinModel.ts](../src/components/widgets/modules/worldClockSkinModel.ts). `WorldClockData` persists only `zones` plus the worn `skin`; Travel Clock is the one schema-extension skin and keeps its home/destination pairing in `skinStates`, while catalogued `renderer-ready` skins hold their controls in component state. Offsets are sampled per instant through `Intl`, so DST and half-hour zones stay correct. The `world_clock` CARD is deleted and its six views came off the picker with it: Timekeeper offers one `world_clock` skin, and `WorldClockWidget` renders whichever view `WorldClockData.skin` holds — which nothing now sets, so every migrated clock lands on City Grid. Re-homing the six as Timekeeper skins is unfinished work, not a decision | [30-world-clock-skins.css](../src/styles/product/30-world-clock-skins.css); the shared analog face is one rotated bar per hand | [worldClockSkinModel.test.ts](../src/components/widgets/modules/worldClockSkinModel.test.ts), then exercise all six views, city add/remove, the working-hour window, the meeting scrubber, the travel pairing, both themes, and reduced motion |
| Imports, PDF/document parsing, generated mind-map topology, media blobs, migration from rival apps | [ImportDocumentModal.tsx](../src/components/ui/ImportDocumentModal.tsx) | [documentReader.ts](../src/utils/documentReader.ts), [pendingImport.ts](../src/utils/pendingImport.ts), [boardDatabase.ts](../src/utils/boardDatabase.ts); import adapter: [mindmapLayout.ts](../src/utils/mindmapLayout.ts), which reuses the parent-graph geometry in [planLayout.ts](../src/utils/planLayout.ts) and creates no group objects. Board exports from rival apps are mapped, never interpreted: `partitionBoardExports` in the modal sniffs each staged file before any model call, and [trelloImport.ts](../src/utils/trelloImport.ts) turns a Trello board into one kanban plus a note or checklist for whatever a kanban card cannot hold, reusing the same `layoutParentGraph` geometry and counting every drop in its report. `.grovepad` remains the only Grovepad board file — a `.json` is admitted solely as a recognized foreign export, and an unrecognized one is refused rather than digested as prose | Import UI locally | [trelloImport.test.ts](../src/utils/trelloImport.test.ts), mind-map layout test, [betaUiContracts.test.ts](../src/components/ui/betaUiContracts.test.ts), build, plus import/cancel/reload smoke and one real rival export dropped on the canvas |
| Media blob sync: dropped pictures, drawing backgrounds, Excalidraw scene files reaching a second device | [mediaSyncService.ts](../src/services/mediaSyncService.ts) | Every blob write outside the export packager goes through `storeMediaBlob` and every read through `loadMediaBlob` ([CanvasViewport.tsx](../src/components/canvas/CanvasViewport.tsx), [excalidrawFiles.ts](../src/utils/excalidrawFiles.ts), [boardCanvasImport.ts](../src/utils/boardCanvasImport.ts), [DrawingWidget.tsx](../src/components/widgets/modules/DrawingWidget.tsx), [MediaWidget.tsx](../src/components/widgets/modules/MediaWidget.tsx), [WidgetRestingFace.tsx](../src/components/widgets/WidgetRestingFace.tsx)); the device's blob store stays the one that must not fail ([boardDatabase.ts](../src/utils/boardDatabase.ts)), and the cloud copy is a consequence — the local write is awaited, the upload is queued. Key enumeration for all three blob families: [widgetMediaKeys.ts](../src/utils/widgetMediaKeys.ts). Objects live at `<canvasId>/<blobKey>` in the private `board-media` bucket; access mirrors canvas ownership and membership ([board media migration](../supabase/migrations/20260729090000_board_media_objects.sql)). Started last in [appRuntime.ts](../src/runtime/appRuntime.ts) so its sweep reads the hydrated board | N/A — no painted surface of its own | [mediaSyncService.test.ts](../src/services/mediaSyncService.test.ts), `npm run check`, then a signed-in two-device smoke: drop an image on one, open the canvas on the other, and confirm an unshared member never receives it |
| Native desktop shell, `.grovepad` file association/icon, OS open-with, all-platform packaging | [src-tauri/src/lib.rs](../src-tauri/src/lib.rs) | Bundle/icon/fileAssociations config: [tauri.conf.json](../src-tauri/tauri.conf.json); macOS bundle extras: [tauri.macos.conf.json](../src-tauri/tauri.macos.conf.json); frontend bridge: [nativeFileOpen.ts](../src/runtime/nativeFileOpen.ts); package parse: [grovepadPackage.ts](../src/utils/grovepadPackage.ts); Canvas-card import/media orchestration: [boardCanvasImport.ts](../src/utils/boardCanvasImport.ts); graph embedding: [boardCanvasEmbedding.ts](../src/utils/boardCanvasEmbedding.ts); iOS Run-from-Xcode build step: [xcode-build-rust.sh](../scripts/ios/xcode-build-rust.sh) with its Tauri settings stand-in [xcodeCliOptionsServer.mjs](../scripts/ios/xcodeCliOptionsServer.mjs); per-device build/signing runbook: [native-apps.md](native-apps.md). Two iOS-only webview fixes live in [lib.rs](../src-tauri/src/lib.rs) because wry exposes no setting for either: `ios_disable_safe_area_insets` stops UIKit insetting the page a second time, and `ios_disable_webview_zoom` pins the scroll view's zoom range to 1 and disables its pinch recognizer — that is the only place the browser's pinch-zoom, double-tap-zoom and focus auto-zoom can be switched off, since WebKit ignores `user-scalable=no`. Both are called from the one `with_webview` hook in `setup` | [LaunchScreen.storyboard](../src-tauri/gen/apple/LaunchScreen.storyboard) is the only native UI surface: a flat `#080a09` matching the `gp-splash-ground` the web splash paints, so launch does not flash. It sits under `gen/apple/`, which `tauri ios init` regenerates — if it is ever white again, restore it rather than re-deriving it | `cargo check` in `src-tauri`, `npm run tauri:dev`, the ladder in [native-apps.md](native-apps.md), open-with smoke |
| Native Note home-screen widget, WidgetKit, Android Glance, device-local selected Note | [nativeNoteWidgetSync.ts](../src/runtime/nativeNoteWidgetSync.ts), [native-widget plugin](../src-tauri/plugins/native-widget), [GrovepadNoteWidget.swift](../src-tauri/native/apple/GrovepadNoteWidget.swift) | Device-only selection/status: [useNativeWidgetStore.ts](../src/store/useNativeWidgetStore.ts); bounded payload contract: [nativeNoteWidget.ts](../src/utils/nativeNoteWidget.ts); validated Tauri command: [src-tauri/src/lib.rs](../src-tauri/src/lib.rs); setup/runbook: [native-os-widgets.md](native-os-widgets.md) | Context-menu action in [WidgetContextMenu.tsx](../src/components/ui/WidgetContextMenu.tsx); native renderers own platform paint | Focused runtime/contract tests, `cargo test`, Android plugin `assembleDebug`, iOS + macOS extension builds; signed-device add/edit/remove smoke |
| Haptic feedback, the Taptic Engine, physical ticks | [haptics.ts](../src/utils/haptics.ts) — the ONE seam every physical tick goes through | iOS route: [native-haptics plugin](../src-tauri/plugins/native-haptics), reached through the `haptic_tap` command in [lib.rs](../src-tauri/src/lib.rs); the name→generator mapping lives in [NativeHapticsPlugin.swift](../src-tauri/plugins/native-haptics/ios/Sources/NativeHapticsPlugin.swift) and nowhere else. WKWebView implements no Vibration API, so the bridge is the only route on Apple hardware; Android and mobile browsers keep `navigator.vibrate`, desktop is a no-op. Host detection is the shared rule in [nativeHost.ts](../src/utils/nativeHost.ts), which [appleSignIn.ts](../src/lib/appleSignIn.ts) also uses — do not add a second copy | N/A — no visual surface | [haptics.test.ts](../src/utils/haptics.test.ts) pins the routing; `cargo build --target aarch64-apple-ios` compiles the Swift; then a real device — a tick has no simulator equivalent |
| Sign in with Apple: native iOS sheet in the app, web OAuth on the site | [appleSignIn.ts](../src/lib/appleSignIn.ts), [LoginPage.tsx](../src/components/auth/LoginPage.tsx) | Native bridge: [NativeAuthPlugin.swift](../src-tauri/plugins/native-auth/ios/Sources/NativeAuthPlugin.swift), reached only through `sign_in_with_apple` in [lib.rs](../src-tauri/src/lib.rs); entitlement in [project.yml](../src-tauri/gen/apple/project.yml). The iOS app hides web-redirect providers because they cannot return to tauri://localhost |  |  |

## Domain routes introduced in Phase 3

| Domain | Implementation route | Contract / pure helpers |
|---|---|---|
| Workspace and canvas hierarchy | [navigationSlice.ts](../src/store/slices/navigationSlice.ts) | [workspaces.ts](../src/types/workspaces.ts) |
| Widget creation and thought plans | [widgetCreationSlice.ts](../src/store/slices/widgetCreationSlice.ts) | [widgetSizing.ts](../src/store/widgetSizing.ts), [widgetSeeds.ts](../src/store/widgetSeeds.ts) |
| Move, resize, auto-scale, untangle, tree compaction, generation floor, drag reflow preview | [widgetLayoutSlice.ts](../src/store/slices/widgetLayoutSlice.ts) | [widgetCollection.ts](../src/store/widgetCollection.ts), [widgetSettling.ts](../src/store/widgetSettling.ts), [generationFloor.ts](../src/store/generationFloor.ts) — the height half of the parenting law, applied to EVERY node: `enforceGenerationFloor` pushes each parent-linked child down until its top clears its parent's bottom by `GENERATION_GAP` (2 cells), measuring a glue cluster as one rigid unit and every other card at the same resting footprint the settle pass separates, carrying a pushed node's whole branch so the shape under it survives, never moving a card up or sideways, never moving a locked one, and bounding parent cycles with a pass limit. `settleWithGenerationFloor` (settle → floor → settle, holding the pushed cards) is the ONE commit path: every committed placement in [widgetLayoutSlice.ts](../src/store/slices/widgetLayoutSlice.ts) goes through it, and so does `addRelation` in [circuitSlice.ts](../src/store/slices/circuitSlice.ts) for a `parent` link — the moment a node becomes a child. Live drag frames deliberately do NOT: the rule lands at the drop, [widgetUntangle.ts](../src/store/widgetUntangle.ts) — two passes behind the one Untangle button: `compactSelectedTrees` pulls each selected parent-linked family into an exact 2-cell lattice (a glue cluster is one node and keeps its weld, siblings and generations sit exactly 2 cells apart, subtree widths are reserved so branches never interleave, and the root holds its place), then `untangleCanvasLayout` separates whatever still overlaps — taking those trees as rigid groups so the push-only pass can never undo the compaction. `untangleCanvas` (whole board, command palette, automations) still only separates. **Live drag reflow is [dragReflow.ts](../src/store/dragReflow.ts), and it never pushes anything.** The cards a drag contends with form an ordered LANE (non-locked clusters sharing at least a quarter of the drag's perpendicular band, in axis order; the axis comes from the smoothed drag direction held by `AXIS_SWITCH_RATIO` hysteresis, so a wobbly diagonal cannot flip which way the lane runs). The drag claims an INDEX in that lane (`claimIndex`, held by `INDEX_HYSTERESIS` = one cell past a card's centre to take the far slot, and a clear cell back to give it up — this is the whole anti-flicker gate), and `laneShifts` recomputes every neighbour's ghost offset FROM THE BASELINE as a pure function of (baseline rects, claimed index, dragged rect): the dragged rect is the hole, cards ordered after it clear its trailing edge, cards ordered before it clear its leading edge, and each run walks outward until it meets a card that already had the room — that card and everything past it never move. Nothing is ever derived from the previous frame, so there is no drift, no chain-depth budget, no area budget and no repair pass; withdrawing the drag recomputes the baseline exactly and a drop there commits nothing. The baseline is snapshotted once per moving set (`buildReflowBaseline`) and measures every cluster EXACTLY as the release settle's `rectFor` does — resting tile, floating title row, group frame band — or the preview and the drop would disagree and the cards would fly twice. `REFLOW_ENGAGE_MS` (120ms of meaningful coverage) is the only timing gate and it latches for the gesture; locked cards and cards outside the band are never lane members, so they are published as `pendingSettleIds` (dimmed, "will settle on drop") and left to the release settle. Ghost offsets live outside `useWidgetStore` — session facts, never history/persistence/sync — and reach the board once, at the drop, through `applyGhostDisplacement` |
| Relations and circuit connections | [circuitSlice.ts](../src/store/slices/circuitSlice.ts) | [relations.ts](../src/types/relations.ts), [fields.ts](../src/widgets/contracts/fields.ts) |
| Glue | [glueSlice.ts](../src/store/slices/glueSlice.ts) | [widgetGraph.ts](../src/store/widgetGraph.ts) |
| Selection, deletion, duplication | [selectionSlice.ts](../src/store/slices/selectionSlice.ts) | [widgetStoreTypes.ts](../src/store/widgetStoreTypes.ts), [widgetDeletion.ts](../src/store/widgetDeletion.ts) |
| Overlays, linking, ghost tree shaper, per-node widget bundles | [uiLinkingSlice.ts](../src/store/slices/uiLinkingSlice.ts), [GhostTreeShaper.tsx](../src/components/canvas/GhostTreeShaper.tsx), selection mode in [AddWidgetModal.tsx](../src/components/ui/AddWidgetModal.tsx) | Multi-root variable bundle layout: [widgetGhostLayout.ts](../src/store/widgetGhostLayout.ts); committed world-space placement: [treeCommitLayout.ts](../src/store/treeCommitLayout.ts) (`clusterLayout` packs a node's widgets weld-adjacent at `GLUE_GAP`, and `commitGhostTree` welds each multi-widget node into one glue cluster — bundles come out as welded clusters, single-widget nodes stay unglued; the bundle snaps to the grid as one rigid unit so seams survive); icon packing, adaptive contours, and alternating accent dashes: [ghostTreePresentation.ts](../src/utils/ghostTreePresentation.ts); staged branch/widget reveal: [treeReveal.ts](../src/store/treeReveal.ts) |
| Undo/redo lifecycle | [widgetHistory.ts](../src/store/widgetHistory.ts) | [boardHydration.test.ts](../src/store/boardHydration.test.ts), [widgetHistory.test.ts](../src/store/widgetHistory.test.ts) |

## High-cost files: search before reading

| File | Search first for | Why it is expensive |
|---|---|---|
| [fields.ts](../src/widgets/fields.ts) | Widget type, field name, command name | Thin composition barrel; descriptors live in the per-domain files under [fields/](../src/widgets/fields) (`coreWidgetFields`, `dataMediaFields`, `studyFields`, `inputLogicFields`, `professionalFields`, `coreCommands`, `valueHelpers`) |
| [registry.ts](../src/widgets/registry.ts) | Widget type or `WidgetDefinition` | Composition barrel; definitions live in the per-domain files under [registry/](../src/widgets/registry) — spread order there IS registry key order, never reorder |
| [EssentialWidgets.tsx](../src/components/widgets/modules/EssentialWidgets.tsx) | Exported widget component name | Re-export barrel; renderers live under [modules/essential/](../src/components/widgets/modules/essential) (`inputWidgets`, `workflowWidgets`, `analysisWidgets`, `opsWidgets`, shared helpers) and still ship as one lazy chunk |
| [renderers](../src/components/widgets/renderers) | Widget type or family id | Family maps own dispatch and lazy loaders without expanding the central shell |
| [WidgetCard.tsx](../src/components/widgets/WidgetCard.tsx) | Interaction name, data attribute, or child component | Shell gestures, ports, and chrome meet here; outline-edge resize lives in [useWidgetResize.ts](../src/components/widgets/useWidgetResize.ts), content-floor measurement in [useContentFloor.ts](../src/components/widgets/useContentFloor.ts) |
| [CanvasViewport.tsx](../src/components/canvas/CanvasViewport.tsx) | Overlay/component name or shortcut | Composition root imports most application surfaces |
| [scenarioResolver.ts](../src/utils/scenarioResolver.ts) | Scenario/archetype name or exported resolver | Classification, routing, preferences, and resolution share one file |
| [product.css](../src/styles/product.css) | Stable `gp-` class or `data-*` selector — search the [product/](../src/styles/product) partials | Ordered `@import` manifest; the cascade spans the six numbered partials, whose order is load-bearing |

## Recurring route: canvas line work

Start with the semantic layer named by the user. Relation, dependency, and wire visuals share [CanvasEdge.tsx](../src/components/canvas/CanvasEdge.tsx) and the `Shared canvas edge paint system` section of [index.css](../src/index.css). Their state models remain separate, and so does wire geometry; relations and dependencies now share ONE routing owner, [edgeRoute.ts](../src/utils/edgeRoute.ts) (a dependency only pins which sides it uses). Inspect [edgeRoute.ts](../src/utils/edgeRoute.ts), [dependencyGeometry.ts](../src/utils/dependencyGeometry.ts), [curve.ts](../src/utils/curve.ts), or [portGeometry.ts](../src/utils/portGeometry.ts) only after identifying the owning semantic layer.

At minimum verify finite SVG geometry, zoom alignment, endpoint side, selection hit target, deletion, and no `NaN`/`Infinity` attributes.

## Recurring route: temporary surfaces (modals, sheets, menus)

Every temporary surface builds on three shared primitives instead of hand-rolling
portal/scrim/Escape scaffolding. [useOverlayDismiss.ts](../src/hooks/useOverlayDismiss.ts)
bundles the overlay count, the focus trap, and window Escape (`escape: false` when
the surface owns richer keyboard handling). [DialogShell.tsx](../src/components/ui/DialogShell.tsx)
adds the portal, `role="dialog"` semantics, and the `gp-scrim` backdrop —
[ConfirmDialog.tsx](../src/components/ui/ConfirmDialog.tsx) is the exemplar.
[ContextMenuSurface.tsx](../src/components/ui/ContextMenuSurface.tsx) is the shared
right-click menu surface (close catcher + viewport-clamped `gp-popup-menu` panel via
[popoverPosition.ts](../src/utils/popoverPosition.ts)); the relation, dependency, and
wire menus all render through it. Small shared helpers live in
[math.ts](../src/utils/math.ts) (`clamp`, `clamp01`), [text.ts](../src/utils/text.ts)
(`truncate`), and tests build fixtures with [factories.ts](../src/test/factories.ts)
(`makeWidget`, `makeRelation`). Visual law stays in the
[popup panel design standard](popup-panel-design.md).

## Recurring route: fullscreen widget editors

A widget whose real interaction surface can't share the app canvas's wheel/pointer
input (a drawing/diagramming tool, anything embedding a heavy third-party editor)
follows the pattern in [modules/excalidraw/](../src/components/widgets/modules/excalidraw):
the in-card renderer is a non-interactive preview/launcher only; the actual editor
mounts exclusively inside a `createPortal(..., document.body)` overlay built on the
shared dismiss primitives (`z-[200]` tier — see the z-index scale used across
`src/components`). Because the portal renders outside the canvas viewport's DOM
subtree, none of [gestureEngine.ts](../src/engine/camera/gestureEngine.ts)'s native
wheel/pointerdown listeners (bound unconditionally on the viewport root) ever see
its input — no `stopPropagation` gymnastics needed. Debounce persistence writes
(`createOwnedTimeout` in [ownedTimeout.ts](../src/utils/ownedTimeout.ts)) rather
than calling `onUpdate` on every change event from a high-frequency editor. A
heavy third-party dependency stays out of the initial bundle by living only
behind the widget's own `lazy()` entry in the matching `renderers/lazy*.ts` file —
no separate manual `import()` wrapper is needed once that boundary exists. Large
embedded binary content (pasted images) never lands in board JSON — write it to
[boardDatabase.ts](../src/utils/boardDatabase.ts)'s media blob store and persist
only a lightweight reference, mirroring `MediaData.localBlobKey`.

## Code review route

1. `git diff --stat` and `git diff --name-only`.
2. Read changed hunks before full files.
3. Find direct callers/importers with `rg -n "from ['\"]<module>|<symbol>" src`.
4. Map each changed behavior to its owner and invariant above.
5. Review interrupted gestures, unmounts, deletion, empty data, reload, reduced motion, and far zoom where relevant.
6. Run the smallest proving test, then expand according to blast radius.

## Verification routes

| Scope | Required command / check |
|---|---|
| Navigation docs only | `npm run docs:check` |
| One pure helper | `npm test -- <nearest-test-file>` plus typecheck and lint |
| Store, persistence, shared types, registry, fields, routing | `npm run check` |
| Production bundle or global CSS | `npm run check:full` plus applicable [manual smoke checklist](manual-smoke-checklist.md) items |

## Keeping the map trustworthy

Update this file in the same commit when:

- An entrypoint moves or is renamed.
- State or contract ownership changes.
- A new recurring subsystem or verification command is introduced.
- A route repeatedly requires opening an unlisted file.

Do not paste implementation into this map. Keep routes, symbols, contracts, and verification only. `npm run docs:check` must remain green.
