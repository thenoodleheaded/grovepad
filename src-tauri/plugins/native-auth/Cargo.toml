[package]
name = "tauri-plugin-native-auth"
version = "0.1.0"
authors = ["Grovepad"]
description = "Grovepad native Sign in with Apple bridge"
edition = "2021"
rust-version = "1.77.2"
links = "tauri-plugin-native-auth"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
tauri = { version = "2.11.3" }
thiserror = "2"

[build-dependencies]
tauri-plugin = { version = "2.6.3", features = ["build"] }
