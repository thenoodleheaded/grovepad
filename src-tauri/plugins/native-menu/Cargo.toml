[package]
name = "tauri-plugin-native-menu"
version = "0.1.0"
authors = ["Grovepad"]
description = "System action sheet bridge for Grovepad context menus"
edition = "2021"
rust-version = "1.77.2"
links = "tauri-plugin-native-menu"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
tauri = { version = "2.11.3" }
thiserror = "2"

[build-dependencies]
tauri-plugin = { version = "2.6.3", features = ["build"] }
